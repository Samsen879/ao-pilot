[{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3572129662","pull_request_review_id":4686432839,"id":3572129662,"node_id":"PRRC_kwDOO5D6p87U6l9-","diff_hunk":"@@ -133,11 +360,1372 @@ def atomic_json(path: Path, value: dict[str, Any], mode: int = 0o644) -> None:\n     )\n \n \n+def _fsync_directory(path: Path) -> None:\n+    descriptor = os.open(path, os.O_RDONLY | os.O_DIRECTORY | os.O_NOFOLLOW)\n+    try:\n+        os.fsync(descriptor)\n+    finally:\n+        os.close(descriptor)\n+\n+\n+def _write_new_file(\n+    path: Path,\n+    payload: bytes,\n+    *,\n+    mode: int,\n+    root_owned: bool,\n+) -> None:\n+    \"\"\"Create a new regular file without following links or replacing evidence.\"\"\"\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    descriptor = os.open(\n+        path,\n+        os.O_WRONLY | os.O_CREAT | os.O_EXCL | os.O_NOFOLLOW,\n+        mode,\n+    )\n+    try:\n+        if root_owned:\n+            os.fchown(descriptor, 0, 0)\n+        view = memoryview(payload)\n+        while view:\n+            written = os.write(descriptor, view)\n+            if written <= 0:\n+                raise ImplementationError(f\"short_write:{path}\")\n+            view = view[written:]\n+        os.fsync(descriptor)\n+    finally:\n+        os.close(descriptor)\n+\n+\n+def _git_run(repo_root: Path, *args: str) -> bytes:\n+    safe_repo_root = repo_root.resolve(strict=True)\n+    environment = {\n+        key: value for key, value in os.environ.items() if not key.startswith(\"GIT_\")\n+    }\n+    environment.update(\n+        {\n+            \"GIT_CONFIG_NOSYSTEM\": \"1\",\n+            \"GIT_CONFIG_GLOBAL\": os.devnull,\n+            \"GIT_OPTIONAL_LOCKS\": \"0\",\n+        }\n+    )\n+    return subprocess.run(\n+        [\n+            \"git\",\n+            \"--no-replace-objects\",\n+            \"-c\",\n+            \"core.hooksPath=/dev/null\",\n+            \"-c\",\n+            f\"safe.directory={safe_repo_root}\",\n+            *args,\n+        ],\n+        cwd=safe_repo_root,\n+        check=True,\n+        stdout=subprocess.PIPE,\n+        stderr=subprocess.PIPE,\n+        env=environment,\n+    ).stdout\n+\n+\n+def _origin_main_sha(repo_root: Path) -> str:\n+    value = _git_run(\n+        repo_root, \"rev-parse\", \"--verify\", \"refs/remotes/origin/main^{commit}\"\n+    ).decode(\"ascii\").strip()\n+    if GIT_SHA_RE.fullmatch(value) is None:\n+        raise ImplementationError(\"origin_main_sha_invalid\")\n+    return value\n+\n+\n+def _git_blob(repo_root: Path, commit_sha: str, relative: str) -> tuple[bytes, str, str]:\n+    path_errors = canonical_repo_path_errors(relative, allowed_prefix=\"\")\n+    if path_errors:\n+        raise ImplementationError(\n+            f\"review_inventory_path_invalid:{relative}:{','.join(path_errors)}\"\n+        )\n+    tree = _git_run(repo_root, \"ls-tree\", \"-z\", \"--full-tree\", commit_sha, \"--\", relative)\n+    records = [record for record in tree.split(b\"\\0\") if record]\n+    if len(records) != 1 or b\"\\t\" not in records[0]:\n+        raise ImplementationError(f\"reviewed_path_missing_or_ambiguous:{relative}\")\n+    header, observed_path = records[0].split(b\"\\t\", 1)\n+    fields = header.decode(\"ascii\").split()\n+    if len(fields) != 3:\n+        raise ImplementationError(f\"reviewed_git_entry_malformed:{relative}\")\n+    mode, object_type, object_id = fields\n+    if observed_path.decode(\"utf-8\") != relative:\n+        raise ImplementationError(f\"reviewed_git_path_mismatch:{relative}\")\n+    if object_type != \"blob\" or mode not in {\"100644\", \"100755\"}:\n+        raise ImplementationError(f\"reviewed_git_entry_not_regular_file:{relative}:{mode}\")\n+    payload = _git_run(repo_root, \"show\", f\"{commit_sha}:{relative}\")\n+    return payload, mode, object_id\n+\n+\n+def _mirror_expected_relative_files() -> set[str]:\n+    return {\n+        *(f\"files/{path}\" for path in REVIEWED_IMPLEMENTATION_PATHS),\n+        *REVIEW_MIRROR_METADATA_FILES,\n+    }\n+\n+\n+def _scan_exact_mirror_files(mirror_root: Path) -> list[str]:\n+    observed: list[str] = []\n+    for path in mirror_root.rglob(\"*\"):\n+        metadata = os.lstat(path)\n+        relative = path.relative_to(mirror_root).as_posix()\n+        if stat.S_ISLNK(metadata.st_mode):\n+            raise ImplementationError(f\"review_mirror_symlink_forbidden:{relative}\")\n+        if stat.S_ISREG(metadata.st_mode):\n+            observed.append(relative)\n+        elif not stat.S_ISDIR(metadata.st_mode):\n+            raise ImplementationError(f\"review_mirror_nonregular_entry:{relative}\")\n+    return sorted(observed)\n+\n+\n+def validate_review_mirror(\n+    mirror_root: Path,\n+    *,\n+    repo_root: Path,\n+    expected_sha: str,\n+    require_current_origin_main: bool = True,\n+    require_root_ownership: bool = False,\n+) -> dict[str, Any]:\n+    \"\"\"Validate an exact-SHA mirror without trusting its stored hashes.\"\"\"\n+    if GIT_SHA_RE.fullmatch(expected_sha) is None:\n+        raise ImplementationError(\"review_mirror_expected_sha_invalid\")\n+    controller_head_sha = _git_run(repo_root, \"rev-parse\", \"HEAD\").decode().strip()\n+    controller_worktree_clean = not bool(\n+        _git_run(repo_root, \"status\", \"--porcelain\").strip()\n+    )\n+    if require_root_ownership and not controller_worktree_clean:\n+        raise ImplementationError(\"review_mirror_controller_worktree_not_clean\")\n+    if require_current_origin_main and _origin_main_sha(repo_root) != expected_sha:\n+        raise ImplementationError(\"review_mirror_stale_origin_main_sha\")\n+    if mirror_root.name != expected_sha or mirror_root.is_symlink() or not mirror_root.is_dir():\n+        raise ImplementationError(\"review_mirror_root_invalid\")\n+    manifest_path = mirror_root / \"reviewed-bundle-manifest.v1.json\"\n+    if manifest_path.is_symlink() or not manifest_path.is_file():\n+        raise ImplementationError(\"review_mirror_manifest_missing_or_symlink\")\n+    manifest_bytes = manifest_path.read_bytes()\n+    manifest = json.loads(manifest_bytes)\n+    if not isinstance(manifest, dict) or manifest.get(\"source_origin_main_sha\") != expected_sha:\n+        raise ImplementationError(\"review_mirror_manifest_source_sha_mismatch\")\n+\n+    expected_artifacts: list[dict[str, Any]] = []\n+    for relative in REVIEWED_IMPLEMENTATION_PATHS:\n+        source_bytes, git_mode, git_object_id = _git_blob(repo_root, expected_sha, relative)\n+        mirrored_path = mirror_root / \"files\" / relative\n+        if mirrored_path.is_symlink() or not mirrored_path.is_file():\n+            raise ImplementationError(f\"review_mirror_file_missing:{relative}\")\n+        mirrored_bytes = mirrored_path.read_bytes()\n+        if mirrored_bytes != source_bytes:\n+            raise ImplementationError(f\"review_mirror_file_hash_mismatch:{relative}\")\n+        metadata = os.lstat(mirrored_path)\n+        if require_root_ownership and (metadata.st_uid != 0 or metadata.st_gid != 0):\n+            raise ImplementationError(f\"review_mirror_not_root_owned:{relative}\")\n+        if stat.S_IMODE(metadata.st_mode) & 0o222:\n+            raise ImplementationError(f\"review_mirror_file_writable:{relative}\")\n+        expected_artifacts.append(\n+            {\n+                \"path\": relative,\n+                \"sha256\": sha256_bytes(source_bytes),\n+                \"byte_length\": len(source_bytes),\n+                \"git_mode\": git_mode,\n+                \"git_object_id\": git_object_id,\n+            }\n+        )\n+    if manifest.get(\"reviewed_artifacts\") != expected_artifacts:\n+        raise ImplementationError(\"review_mirror_manifest_artifact_inventory_mismatch\")\n+    hash_entries = [\n+        {\"path\": item[\"path\"], \"sha256\": item[\"sha256\"]}\n+        for item in expected_artifacts\n+    ]\n+    bundle_hash = reviewed_bundle_hash(hash_entries)\n+    if manifest.get(\"reviewed_bundle_hash\") != bundle_hash:\n+        raise ImplementationError(\"review_mirror_bundle_hash_mismatch\")\n+    observed = set(_scan_exact_mirror_files(mirror_root))\n+    expected = _mirror_expected_relative_files()\n+    if observed != expected:\n+        raise ImplementationError(\n+            \"review_mirror_exact_set_mismatch:\"\n+            f\"missing={sorted(expected-observed)}:extra={sorted(observed-expected)}\"\n+        )\n+    source_sha_path = mirror_root / \"source-origin-main-sha\"\n+    if source_sha_path.read_bytes() != (expected_sha + \"\\n\").encode(\"ascii\"):\n+        raise ImplementationError(\"review_mirror_source_sha_bytes_mismatch\")\n+    expected_inventory = {\n+        \"schema_version\": \"parent_creation_review_inventory.v1\",\n+        \"source_origin_main_sha\": expected_sha,\n+        \"paths\": list(REVIEWED_IMPLEMENTATION_PATHS),\n+        \"path_count\": len(REVIEWED_IMPLEMENTATION_PATHS),\n+        \"inventory_hash\": canonical_sha256(list(REVIEWED_IMPLEMENTATION_PATHS)),\n+    }\n+    inventory_path = mirror_root / \"reviewed-inventory.v1.json\"\n+    inventory_bytes = inventory_path.read_bytes()\n+    if json.loads(inventory_bytes) != expected_inventory:\n+        raise ImplementationError(\"review_mirror_inventory_mismatch\")\n+    producer_acl_evidence_only = manifest.get(\"producer_acl_evidence_only\")\n+    if not isinstance(producer_acl_evidence_only, bool):\n+        raise ImplementationError(\"review_mirror_producer_acl_evidence_flag_invalid\")\n+    if controller_head_sha != expected_sha and not producer_acl_evidence_only:\n+        raise ImplementationError(\"review_mirror_hybrid_controller_not_explicitly_scoped\")\n+    binding_eligible = (\n+        controller_worktree_clean\n+        and controller_head_sha == expected_sha\n+        and not producer_acl_evidence_only\n+    )\n+    if manifest.get(\"independent_review_binding_eligible\") is not binding_eligible:\n+        raise ImplementationError(\"review_mirror_binding_eligibility_mismatch\")\n+    expected_manifest = {\n+        \"schema_version\": \"parent_creation_review_bundle_manifest.v1\",\n+        \"source_ref\": \"origin/main\",\n+        \"controller_repo_head_sha\": controller_head_sha,\n+        \"controller_worktree_clean\": controller_worktree_clean,\n+        \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+        \"independent_review_binding_eligible\": binding_eligible,\n+        \"source_origin_main_sha\": expected_sha,\n+        \"reviewed_path_count\": len(expected_artifacts),\n+        \"reviewed_bundle_hash\": bundle_hash,\n+        \"reviewed_artifacts\": expected_artifacts,\n+    }\n+    if manifest != expected_manifest:\n+        raise ImplementationError(\"review_mirror_manifest_mismatch\")\n+    materialization_path = mirror_root / \"materialization-receipt.v1.json\"\n+    materialization_bytes = materialization_path.read_bytes()\n+    materialization = json.loads(materialization_bytes)\n+    required_materialization = {\n+        \"schema_version\": \"parent_creation_review_mirror_materialization_receipt.v1\",\n+        \"materializer_effective_uid\": 0 if require_root_ownership else os.geteuid(),\n+        \"source_ref\": \"origin/main\",\n+        \"controller_repo_head_sha\": controller_head_sha,\n+        \"controller_worktree_clean\": controller_worktree_clean,\n+        \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+        \"independent_review_binding_eligible\": binding_eligible,\n+        \"source_origin_main_sha_before\": expected_sha,\n+        \"source_origin_main_sha\": expected_sha,\n+        \"source_origin_main_sha_after\": expected_sha,\n+        \"mirror_root\": str(mirror_root),\n+        \"reviewed_path_count\": len(expected_artifacts),\n+        \"reviewed_inventory_hash\": expected_inventory[\"inventory_hash\"],\n+        \"reviewed_bundle_hash\": bundle_hash,\n+        \"source_sha_file_sha256\": sha256_bytes(source_sha_path.read_bytes()),\n+        \"reviewed_inventory_file_sha256\": sha256_bytes(inventory_bytes),\n+        \"reviewed_manifest_file_sha256\": sha256_bytes(manifest_bytes),\n+        \"root_owned\": require_root_ownership,\n+        \"atomic_staging_publish\": True,\n+        \"files_fsynced\": True,\n+        \"path_traversal_rejected\": True,\n+        \"symlinks_rejected\": True,\n+        \"exact_set_required\": True,\n+        \"exact_set_verified\": True,\n+        \"hashes_verified\": True,\n+        \"symlink_scan_passed\": True,\n+        \"path_traversal_scan_passed\": True,\n+        \"extra_file_scan_passed\": True,\n+        \"producer_can_modify_after_materialization\": False,\n+        \"validation_results\": {\n+            \"source_sha_exact\": True,\n+            \"inventory_exact\": True,\n+            \"per_path_git_bytes_exact\": True,\n+            \"bundle_hash_recomputed\": True,\n+            \"missing_extra_symlink_rejected\": True,\n+        },\n+        \"does_not_constitute_independent_review_receipt\": True,\n+        \"provider_calls\": 0,\n+        \"remote_data_writes\": 0,\n+        \"campaign_a_started\": False,\n+    }\n+    if not isinstance(materialization, dict):\n+        raise ImplementationError(\"review_mirror_materialization_receipt_invalid\")\n+    for field, expected_value in required_materialization.items():\n+        if materialization.get(field) != expected_value:\n+            raise ImplementationError(\n+                f\"review_mirror_materialization_receipt_field_mismatch:{field}\"\n+            )\n+    timestamp = materialization.get(\"materialized_at\")\n+    try:\n+        parsed_timestamp = datetime.fromisoformat(\n+            timestamp[:-1] + \"+00:00\"\n+            if isinstance(timestamp, str) and timestamp.endswith(\"Z\")\n+            else str(timestamp)\n+        )\n+    except ValueError:\n+        parsed_timestamp = None\n+    if (\n+        not isinstance(timestamp, str)\n+        or not timestamp.endswith(\"Z\")\n+        or parsed_timestamp is None\n+        or parsed_timestamp.tzinfo is None\n+    ):\n+        raise ImplementationError(\"review_mirror_materialization_timestamp_invalid\")\n+    for path in (mirror_root, *mirror_root.rglob(\"*\")):\n+        metadata = os.lstat(path)\n+        if require_root_ownership and (metadata.st_uid, metadata.st_gid) != (0, 0):\n+            raise ImplementationError(f\"review_mirror_not_root_owned:{path}\")\n+        expected_mode = 0o550 if stat.S_ISDIR(metadata.st_mode) else 0o440\n+        if stat.S_IMODE(metadata.st_mode) != expected_mode:\n+            raise ImplementationError(f\"review_mirror_mode_mismatch:{path}\")\n+    return {\n+        \"schema_version\": \"parent_creation_review_mirror_readback.v1\",\n+        \"source_origin_main_sha\": expected_sha,\n+        \"mirror_root\": str(mirror_root),\n+        \"reviewed_path_count\": len(expected_artifacts),\n+        \"reviewed_bundle_hash\": bundle_hash,\n+        \"materialization_receipt_sha256\": sha256_bytes(materialization_bytes),\n+        \"exact_set_verified\": True,\n+        \"hashes_verified\": True,\n+        \"symlink_count\": 0,\n+        \"materialization_passed\": True,\n+        \"does_not_constitute_independent_review_receipt\": True,\n+    }\n+\n+\n+def materialize_review_mirror(\n+    *,\n+    repo_root: Path,\n+    durable_root: Path,\n+    expected_sha: str | None = None,\n+    require_root: bool = True,\n+    producer_acl_evidence_only: bool = False,\n+) -> dict[str, Any]:\n+    \"\"\"Materialize reviewed Git blobs into an immutable exact-SHA staging tree.\"\"\"\n+    if require_root:\n+        ensure_root()\n+    controller_head_sha = _git_run(repo_root, \"rev-parse\", \"HEAD\").decode().strip()\n+    controller_worktree_clean = not bool(\n+        _git_run(repo_root, \"status\", \"--porcelain\").strip()\n+    )\n+    if require_root and not controller_worktree_clean:\n+        raise ImplementationError(\"review_mirror_controller_worktree_not_clean\")\n+    source_sha_before = _origin_main_sha(repo_root)\n+    if expected_sha is not None and source_sha_before != expected_sha:\n+        raise ImplementationError(\"requested_review_sha_is_not_current_origin_main\")\n+    source_sha = expected_sha or source_sha_before\n+    if controller_head_sha != source_sha and not producer_acl_evidence_only:\n+        raise ImplementationError(\n+            \"review_mirror_controller_head_must_equal_source_sha_unless_producer_acl_evidence_only\"\n+        )\n+    mirror_parent = durable_root / REVIEW_INPUT_RELATIVE_ROOT\n+    if mirror_parent.is_symlink():\n+        raise ImplementationError(\"review_mirror_parent_symlink_forbidden\")\n+    mirror_parent.mkdir(parents=True, exist_ok=True)\n+    final_root = mirror_parent / source_sha\n+    if final_root.exists() or final_root.is_symlink():\n+        raise ImplementationError(\"review_mirror_final_path_already_exists\")\n+    staging = Path(tempfile.mkdtemp(prefix=f\".{source_sha}.\", dir=mirror_parent))\n+    if require_root:\n+        os.chown(staging, 0, 0)\n+    try:\n+        artifacts: list[dict[str, Any]] = []\n+        for relative in REVIEWED_IMPLEMENTATION_PATHS:\n+            source_bytes, git_mode, git_object_id = _git_blob(repo_root, source_sha, relative)\n+            target = staging / \"files\" / relative\n+            _write_new_file(target, source_bytes, mode=0o440, root_owned=require_root)\n+            artifacts.append(\n+                {\n+                    \"path\": relative,\n+                    \"sha256\": sha256_bytes(source_bytes),\n+                    \"byte_length\": len(source_bytes),\n+                    \"git_mode\": git_mode,\n+                    \"git_object_id\": git_object_id,\n+                }\n+            )\n+        hash_entries = [\n+            {\"path\": item[\"path\"], \"sha256\": item[\"sha256\"]} for item in artifacts\n+        ]\n+        bundle_hash = reviewed_bundle_hash(hash_entries)\n+        inventory = {\n+            \"schema_version\": \"parent_creation_review_inventory.v1\",\n+            \"source_origin_main_sha\": source_sha,\n+            \"paths\": list(REVIEWED_IMPLEMENTATION_PATHS),\n+            \"path_count\": len(REVIEWED_IMPLEMENTATION_PATHS),\n+            \"inventory_hash\": canonical_sha256(list(REVIEWED_IMPLEMENTATION_PATHS)),\n+        }\n+        manifest = {\n+            \"schema_version\": \"parent_creation_review_bundle_manifest.v1\",\n+            \"source_ref\": \"origin/main\",\n+            \"controller_repo_head_sha\": controller_head_sha,\n+            \"controller_worktree_clean\": controller_worktree_clean,\n+            \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+            \"independent_review_binding_eligible\": (\n+                controller_worktree_clean\n+                and controller_head_sha == source_sha\n+                and not producer_acl_evidence_only\n+            ),\n+            \"source_origin_main_sha\": source_sha,\n+            \"reviewed_path_count\": len(artifacts),\n+            \"reviewed_bundle_hash\": bundle_hash,\n+            \"reviewed_artifacts\": artifacts,\n+        }\n+        source_sha_payload = (source_sha + \"\\n\").encode(\"ascii\")\n+        inventory_payload = (\n+            json.dumps(inventory, ensure_ascii=False, indent=2) + \"\\n\"\n+        ).encode()\n+        manifest_payload = (\n+            json.dumps(manifest, ensure_ascii=False, indent=2) + \"\\n\"\n+        ).encode()\n+        _write_new_file(\n+            staging / \"source-origin-main-sha\",\n+            source_sha_payload,\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        _write_new_file(\n+            staging / \"reviewed-inventory.v1.json\",\n+            inventory_payload,\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        _write_new_file(\n+            staging / \"reviewed-bundle-manifest.v1.json\",\n+            manifest_payload,\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        source_sha_after = _origin_main_sha(repo_root)\n+        if source_sha_after != source_sha_before:\n+            raise ImplementationError(\"origin_main_changed_during_review_materialization\")\n+        receipt = {\n+            \"schema_version\": \"parent_creation_review_mirror_materialization_receipt.v1\",\n+            \"materialized_at\": utc_now(),\n+            \"materializer_effective_uid\": os.geteuid(),\n+            \"source_ref\": \"origin/main\",\n+            \"controller_repo_head_sha\": controller_head_sha,\n+            \"controller_worktree_clean\": controller_worktree_clean,\n+            \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+            \"independent_review_binding_eligible\": (\n+                controller_worktree_clean\n+                and controller_head_sha == source_sha\n+                and not producer_acl_evidence_only\n+            ),\n+            \"source_origin_main_sha_before\": source_sha_before,\n+            \"source_origin_main_sha\": source_sha,\n+            \"source_origin_main_sha_after\": source_sha_after,\n+            \"mirror_root\": str(final_root),\n+            \"reviewed_path_count\": len(artifacts),\n+            \"reviewed_inventory_hash\": inventory[\"inventory_hash\"],\n+            \"reviewed_bundle_hash\": bundle_hash,\n+            \"source_sha_file_sha256\": sha256_bytes(source_sha_payload),\n+            \"reviewed_inventory_file_sha256\": sha256_bytes(inventory_payload),\n+            \"reviewed_manifest_file_sha256\": sha256_bytes(manifest_payload),\n+            \"root_owned\": require_root,\n+            \"atomic_staging_publish\": True,\n+            \"files_fsynced\": True,\n+            \"path_traversal_rejected\": True,\n+            \"symlinks_rejected\": True,\n+            \"exact_set_required\": True,\n+            \"exact_set_verified\": True,\n+            \"hashes_verified\": True,\n+            \"symlink_scan_passed\": True,\n+            \"path_traversal_scan_passed\": True,\n+            \"extra_file_scan_passed\": True,\n+            \"producer_can_modify_after_materialization\": False,\n+            \"validation_results\": {\n+                \"source_sha_exact\": True,\n+                \"inventory_exact\": True,\n+                \"per_path_git_bytes_exact\": True,\n+                \"bundle_hash_recomputed\": True,\n+                \"missing_extra_symlink_rejected\": True,\n+            },\n+            \"does_not_constitute_independent_review_receipt\": True,\n+            \"provider_calls\": 0,\n+            \"remote_data_writes\": 0,\n+            \"campaign_a_started\": False,\n+        }\n+        _write_new_file(\n+            staging / \"materialization-receipt.v1.json\",\n+            (json.dumps(receipt, ensure_ascii=False, indent=2) + \"\\n\").encode(),\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        for path in sorted(staging.rglob(\"*\"), reverse=True):\n+            if path.is_dir():\n+                if require_root:\n+                    os.chown(path, 0, 0)\n+                os.chmod(path, 0o550)\n+        os.chmod(staging, 0o550)\n+        _fsync_directory(staging)\n+        os.replace(staging, final_root)\n+        _fsync_directory(mirror_parent)\n+        validate_review_mirror(\n+            final_root,\n+            repo_root=repo_root,\n+            expected_sha=source_sha,\n+            require_current_origin_main=True,\n+            require_root_ownership=require_root,\n+        )\n+        return receipt\n+    except Exception:\n+        # Preserve partial evidence; never delete or overwrite it.\n+        if staging.exists():\n+            quarantine = mirror_parent / f\"failed-{staging.name.removeprefix('.')}\"\n+            if not quarantine.exists():\n+                os.replace(staging, quarantine)\n+        raise\n+\n+\n def ensure_root() -> None:\n     if os.geteuid() != 0:\n         raise ImplementationError(\"root_required\")\n \n \n+def reviewer_remediation_plan(\n+    *, durable_root: Path = Path(DURABLE_ROOT), state_root: Path = Path(STATE_ROOT)\n+) -> dict[str, Any]:\n+    return {\n+        \"schema_version\": \"cie9709_parent_creation_reviewer_remediation_plan.v1\",\n+        \"durable_root\": str(durable_root),\n+        \"state_root\": str(state_root),\n+        \"review_input_root\": str(durable_root / REVIEW_INPUT_RELATIVE_ROOT),\n+        \"reviewer_observation_root\": str(\n+            durable_root / \"role-inbox\" / REVIEWER_ROLE_ID\n+        ),\n+        \"reviewer_checkpoint_root\": str(\n+            durable_root / \"checkpoints\" / REVIEWER_ROLE_ID\n+        ),\n+        \"acl_grants_to_remove\": [\n+            str(durable_root / relative)\n+            for relative in REVIEWER_ACL_REMOVE_RELATIVE_PATHS\n+        ],\n+        \"managed_scope_acl_enforcement\": {\n+            \"existing_unlisted_directories\": \"named_reviewer_access_and_default_acl_none\",\n+            \"required_traverse_only\": [\n+                str(durable_root),\n+                str(durable_root / \"review-inputs\"),\n+                str(durable_root / \"role-inbox\"),\n+                str(durable_root / \"checkpoints\"),\n+            ],\n+            \"review_input_subtree\": \"named_reviewer_read_execute_no_write\",\n+            \"own_observation_and_checkpoint\": \"reviewer_owner_0700_default_owner_only\",\n+            \"other_principals_permissions_unchanged\": True,\n+        },\n+        \"ledger_broker_runtime_remediation\": {\n+            \"contract\": ledger_broker_runtime_contract(),\n+            \"source_unit\": str(ROOT / LEDGER_SOCKET_UNIT_REPO_PATH),\n+            \"runtime_unit\": LEDGER_SOCKET_UNIT_RUNTIME_PATH,\n+            \"pre_change_unit_archived\": True,\n+            \"atomic_unit_replace\": True,\n+            \"daemon_reload_and_socket_restart\": True,\n+            \"runtime_directory_snapshot_separate_from_reviewer_managed_scope\": True,\n+        },\n+        \"normalized_reviewer_access\": reviewer_access_contract(),\n+        \"incremental_only\": True,\n+        \"pristine_apply_invoked\": False,\n+        \"destructive_delete_available\": False,\n+        \"rollback_strategy\": \"restore_exact_acl_snapshot_and_quarantine_new_paths\",\n+    }\n+\n+\n+def _metadata_receipt(path: Path) -> dict[str, Any]:\n+    if not path.exists() and not path.is_symlink():\n+        return {\"path\": str(path), \"state\": \"absent\"}\n+    metadata = os.lstat(path)\n+    return {\n+        \"path\": str(path),\n+        \"state\": \"present\",\n+        \"uid\": metadata.st_uid,\n+        \"gid\": metadata.st_gid,\n+        \"mode\": f\"{stat.S_IMODE(metadata.st_mode):04o}\",\n+        \"is_directory\": stat.S_ISDIR(metadata.st_mode),\n+        \"is_symlink\": stat.S_ISLNK(metadata.st_mode),\n+    }\n+\n+\n+def _reviewer_touched_paths(durable_root: Path) -> list[Path]:\n+    paths = [\n+        durable_root / \"review-inputs\",\n+        durable_root / REVIEW_INPUT_RELATIVE_ROOT,\n+        durable_root / REVIEWER_OBSERVATION_RELATIVE_ROOT,\n+        durable_root / REVIEWER_CHECKPOINT_RELATIVE_ROOT,\n+        *(durable_root / relative for relative in REVIEWER_ACL_REMOVE_RELATIVE_PATHS),\n+    ]\n+    for current, directories, _ in os.walk(durable_root, followlinks=False):\n+        current_path = Path(current)\n+        if current_path.is_symlink():\n+            directories[:] = []\n+            continue\n+        paths.append(current_path)\n+        directories[:] = [\n+            name for name in directories if not (current_path / name).is_symlink()\n+        ]\n+    return sorted(set(paths))\n+\n+\n+def _reviewer_remediation_snapshot_paths(durable_root: Path) -> list[Path]:\n+    \"\"\"Include runtime trust paths without applying reviewer ACLs outside /srv.\"\"\"\n+    return sorted(\n+        {\n+            *_reviewer_touched_paths(durable_root),\n+            Path(RUN_ROOT),\n+            Path(LEDGER_SOCKET_UNIT_RUNTIME_PATH),\n+        }\n+    )\n+\n+\n+def _apply_reviewer_managed_scope_default_deny(durable_root: Path) -> dict[str, Any]:\n+    \"\"\"Give UID 977 only the exact traversals and directories in its contract.\"\"\"\n+    reviewer = pwd.getpwnam(REVIEWER_USER)\n+    review_inputs = durable_root / \"review-inputs\"\n+    review_parent = durable_root / REVIEW_INPUT_RELATIVE_ROOT\n+    observation_parent = durable_root / \"role-inbox\"\n+    checkpoint_parent = durable_root / \"checkpoints\"\n+    own_roots = {\n+        durable_root / REVIEWER_OBSERVATION_RELATIVE_ROOT,\n+        durable_root / REVIEWER_CHECKPOINT_RELATIVE_ROOT,\n+    }\n+    traverse_only = {\n+        durable_root,\n+        review_inputs,\n+        observation_parent,\n+        checkpoint_parent,\n+    }\n+    denied: list[str] = []\n+    traversed: list[str] = []\n+    reviewed: list[str] = []\n+    owned: list[str] = []\n+    for path in _reviewer_touched_paths(durable_root):\n+        if not path.exists():\n+            continue\n+        if path.is_symlink() or not path.is_dir():\n+            raise ImplementationError(f\"reviewer_managed_scope_path_unsafe:{path}\")\n+        if path in own_roots or any(root in path.parents for root in own_roots):\n+            if path in own_roots:\n+                normalize_reviewer_output_acl(path)\n+            owned.append(str(path))\n+            continue\n+        if path == review_parent or review_parent in path.parents:\n+            run([\"setfacl\", \"-m\", f\"u:{REVIEWER_USER}:rx\", str(path)])\n+            run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:rx\", str(path)])\n+            reviewed.append(str(path))\n+            continue\n+        if path in traverse_only:\n+            run([\"setfacl\", \"-m\", f\"u:{REVIEWER_USER}:x\", str(path)])\n+            traversed.append(str(path))\n+            continue\n+        run([\"setfacl\", \"-m\", f\"u:{REVIEWER_USER}:---\", str(path)])\n+        run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:---\", str(path)])\n+        denied.append(str(path))\n+\n+    # Make future direct children fail closed for this principal.  Existing\n+    # default ACL base entries are preserved by setfacl; owner semantics on the\n+    # reviewer's own child directories still take precedence over named-user ACLs.\n+    for path in (durable_root, review_inputs, observation_parent, checkpoint_parent):\n+        run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:---\", str(path)])\n+    run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:rx\", str(review_parent)])\n+    for path in own_roots:\n+        metadata = os.lstat(path)\n+        if (metadata.st_uid, metadata.st_gid) != (reviewer.pw_uid, reviewer.pw_gid):\n+            raise ImplementationError(f\"reviewer_output_owner_mismatch:{path}\")\n+        normalize_reviewer_output_acl(path)\n+    return {\n+        \"explicit_deny_paths\": denied,\n+        \"traverse_only_paths\": traversed,\n+        \"review_mirror_read_paths\": reviewed,\n+        \"owner_only_output_paths\": owned,\n+        \"unlisted_managed_directories_default_deny\": True,\n+    }\n+\n+\n+def _capture_acl_snapshot(paths: list[Path]) -> bytes:\n+    existing = [str(path) for path in paths if path.exists() and not path.is_symlink()]\n+    if not existing:\n+        return b\"\"\n+    return run([\"getfacl\", \"-p\", *existing]).stdout\n+\n+\n+def _archive_prior_file(path: Path, receipt_root: Path) -> dict[str, Any]:\n+    if path.is_symlink() or not path.is_file():\n+        raise ImplementationError(f\"superseded_runtime_file_invalid:{path}\")\n+    payload = path.read_bytes()\n+    target = receipt_root / f\"prior-{path.name}\"\n+    _write_new_file(target, payload, mode=0o600, root_owned=True)\n+    return {\n+        \"runtime_path\": str(path),\n+        \"archive_path\": str(target),\n+        \"prior_sha256\": sha256_bytes(payload),\n+        \"prior_byte_length\": len(payload),\n+    }\n+\n+\n+def normalize_reviewer_output_acl(path: Path) -> None:\n+    \"\"\"Conserve owner-only access/default ACLs without widening mode via a mask.\"\"\"\n+    run([\"setfacl\", \"-b\", str(path)])\n+    run([\"setfacl\", \"-k\", str(path)])\n+    run(\n+        [\n+            \"setfacl\",\n+            \"-d\",\n+            \"-m\",\n+            \"u::rwx,g::---,o::---\",\n+            str(path),\n+        ]\n+    )\n+    os.chmod(path, 0o700)\n+    if stat.S_IMODE(os.lstat(path).st_mode) != 0o700:\n+        raise ImplementationError(f\"reviewer_output_mode_not_0700:{path}\")\n+\n+\n+def apply_reviewer_remediation(\n+    *, durable_root: Path = Path(DURABLE_ROOT), state_root: Path = Path(STATE_ROOT)\n+) -> dict[str, Any]:\n+    \"\"\"Incrementally repair only the exact-SHA reviewer ACL surface.\"\"\"\n+    ensure_root()\n+    if durable_root.is_symlink() or not durable_root.is_dir():\n+        raise ImplementationError(\"reviewer_remediation_durable_root_invalid\")\n+    controller_head_sha = _git_run(ROOT, \"rev-parse\", \"HEAD\").decode().strip()\n+    controller_origin_main_sha = _origin_main_sha(ROOT)\n+    if _git_run(ROOT, \"status\", \"--porcelain\").strip():\n+        raise ImplementationError(\"reviewer_remediation_controller_worktree_not_clean\")\n+    _assert_ledger_runtime_root_safe_for_adoption()\n+    source_socket_unit = ROOT / LEDGER_SOCKET_UNIT_REPO_PATH\n+    runtime_socket_unit = Path(LEDGER_SOCKET_UNIT_RUNTIME_PATH)\n+    for path in (source_socket_unit, runtime_socket_unit):\n+        if path.is_symlink() or not path.is_file():\n+            raise ImplementationError(f\"ledger_socket_unit_unsafe:{path}\")","path":"scripts/vlm/implement_cie9709_host_v1.py","commit_id":"b3c4b9e773b015bb9f2eb6c86a82a76d55306b21","original_commit_id":"29d8eb7b0670f6dffe9c15f59aaa40b41c8e91d2","user":{"login":"gemini-code-assist[bot]","id":176961590,"node_id":"BOT_kgDOCow4Ng","avatar_url":"https://avatars.githubusercontent.com/in/956858?v=4","gravatar_id":"","url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D","html_url":"https://github.com/apps/gemini-code-assist","followers_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/followers","following_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/following{/other_user}","gists_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/gists{/gist_id}","starred_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/subscriptions","organizations_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/orgs","repos_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/repos","events_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/events{/privacy}","received_events_url":"https://api.github.com/users/gemini-code-assist%5Bbot%5D/received_events","type":"Bot","user_view_type":"public","site_admin":false},"body":"![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)\n\nThis large block of code, containing `reviewer_access_contract`, `_reviewer_policy_grants`, `_reviewer_managed_scope_acl_scan_errors`, and `reviewer_access_conservation_errors`, appears to be a duplicate of the code added in `scripts/vlm/cie9709_host_contract_v1.py`.\n\nTo improve maintainability and avoid future inconsistencies, these functions should be defined in a single, canonical location (likely `cie9709_host_contract_v1.py`) and imported where needed. This adheres to the Don't Repeat Yourself (DRY) principle and will make the codebase easier to manage.","created_at":"2026-07-13T15:57:50Z","updated_at":"2026-07-13T15:57:51Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1546#discussion_r3572129662","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1546","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3572129662"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1546#discussion_r3572129662"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1546"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3572129662/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":490,"original_start_line":490,"start_side":"RIGHT","line":1096,"original_line":1081,"side":"RIGHT","author_association":"NONE","original_position":1002,"position":1017,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3572170505","pull_request_review_id":4686482443,"id":3572170505,"node_id":"PRRC_kwDOO5D6p87U6v8J","diff_hunk":"@@ -133,11 +360,1372 @@ def atomic_json(path: Path, value: dict[str, Any], mode: int = 0o644) -> None:\n     )\n \n \n+def _fsync_directory(path: Path) -> None:\n+    descriptor = os.open(path, os.O_RDONLY | os.O_DIRECTORY | os.O_NOFOLLOW)\n+    try:\n+        os.fsync(descriptor)\n+    finally:\n+        os.close(descriptor)\n+\n+\n+def _write_new_file(\n+    path: Path,\n+    payload: bytes,\n+    *,\n+    mode: int,\n+    root_owned: bool,\n+) -> None:\n+    \"\"\"Create a new regular file without following links or replacing evidence.\"\"\"\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    descriptor = os.open(\n+        path,\n+        os.O_WRONLY | os.O_CREAT | os.O_EXCL | os.O_NOFOLLOW,\n+        mode,\n+    )\n+    try:\n+        if root_owned:\n+            os.fchown(descriptor, 0, 0)\n+        view = memoryview(payload)\n+        while view:\n+            written = os.write(descriptor, view)\n+            if written <= 0:\n+                raise ImplementationError(f\"short_write:{path}\")\n+            view = view[written:]\n+        os.fsync(descriptor)\n+    finally:\n+        os.close(descriptor)\n+\n+\n+def _git_run(repo_root: Path, *args: str) -> bytes:\n+    safe_repo_root = repo_root.resolve(strict=True)\n+    environment = {\n+        key: value for key, value in os.environ.items() if not key.startswith(\"GIT_\")\n+    }\n+    environment.update(\n+        {\n+            \"GIT_CONFIG_NOSYSTEM\": \"1\",\n+            \"GIT_CONFIG_GLOBAL\": os.devnull,\n+            \"GIT_OPTIONAL_LOCKS\": \"0\",\n+        }\n+    )\n+    return subprocess.run(\n+        [\n+            \"git\",\n+            \"--no-replace-objects\",\n+            \"-c\",\n+            \"core.hooksPath=/dev/null\",\n+            \"-c\",\n+            f\"safe.directory={safe_repo_root}\",\n+            *args,\n+        ],\n+        cwd=safe_repo_root,\n+        check=True,\n+        stdout=subprocess.PIPE,\n+        stderr=subprocess.PIPE,\n+        env=environment,\n+    ).stdout\n+\n+\n+def _origin_main_sha(repo_root: Path) -> str:\n+    value = _git_run(\n+        repo_root, \"rev-parse\", \"--verify\", \"refs/remotes/origin/main^{commit}\"\n+    ).decode(\"ascii\").strip()\n+    if GIT_SHA_RE.fullmatch(value) is None:\n+        raise ImplementationError(\"origin_main_sha_invalid\")\n+    return value\n+\n+\n+def _git_blob(repo_root: Path, commit_sha: str, relative: str) -> tuple[bytes, str, str]:\n+    path_errors = canonical_repo_path_errors(relative, allowed_prefix=\"\")\n+    if path_errors:\n+        raise ImplementationError(\n+            f\"review_inventory_path_invalid:{relative}:{','.join(path_errors)}\"\n+        )\n+    tree = _git_run(repo_root, \"ls-tree\", \"-z\", \"--full-tree\", commit_sha, \"--\", relative)\n+    records = [record for record in tree.split(b\"\\0\") if record]\n+    if len(records) != 1 or b\"\\t\" not in records[0]:\n+        raise ImplementationError(f\"reviewed_path_missing_or_ambiguous:{relative}\")\n+    header, observed_path = records[0].split(b\"\\t\", 1)\n+    fields = header.decode(\"ascii\").split()\n+    if len(fields) != 3:\n+        raise ImplementationError(f\"reviewed_git_entry_malformed:{relative}\")\n+    mode, object_type, object_id = fields\n+    if observed_path.decode(\"utf-8\") != relative:\n+        raise ImplementationError(f\"reviewed_git_path_mismatch:{relative}\")\n+    if object_type != \"blob\" or mode not in {\"100644\", \"100755\"}:\n+        raise ImplementationError(f\"reviewed_git_entry_not_regular_file:{relative}:{mode}\")\n+    payload = _git_run(repo_root, \"show\", f\"{commit_sha}:{relative}\")\n+    return payload, mode, object_id\n+\n+\n+def _mirror_expected_relative_files() -> set[str]:\n+    return {\n+        *(f\"files/{path}\" for path in REVIEWED_IMPLEMENTATION_PATHS),\n+        *REVIEW_MIRROR_METADATA_FILES,\n+    }\n+\n+\n+def _scan_exact_mirror_files(mirror_root: Path) -> list[str]:\n+    observed: list[str] = []\n+    for path in mirror_root.rglob(\"*\"):\n+        metadata = os.lstat(path)\n+        relative = path.relative_to(mirror_root).as_posix()\n+        if stat.S_ISLNK(metadata.st_mode):\n+            raise ImplementationError(f\"review_mirror_symlink_forbidden:{relative}\")\n+        if stat.S_ISREG(metadata.st_mode):\n+            observed.append(relative)\n+        elif not stat.S_ISDIR(metadata.st_mode):\n+            raise ImplementationError(f\"review_mirror_nonregular_entry:{relative}\")\n+    return sorted(observed)\n+\n+\n+def validate_review_mirror(\n+    mirror_root: Path,\n+    *,\n+    repo_root: Path,\n+    expected_sha: str,\n+    require_current_origin_main: bool = True,\n+    require_root_ownership: bool = False,\n+) -> dict[str, Any]:\n+    \"\"\"Validate an exact-SHA mirror without trusting its stored hashes.\"\"\"\n+    if GIT_SHA_RE.fullmatch(expected_sha) is None:\n+        raise ImplementationError(\"review_mirror_expected_sha_invalid\")\n+    controller_head_sha = _git_run(repo_root, \"rev-parse\", \"HEAD\").decode().strip()\n+    controller_worktree_clean = not bool(\n+        _git_run(repo_root, \"status\", \"--porcelain\").strip()\n+    )\n+    if require_root_ownership and not controller_worktree_clean:\n+        raise ImplementationError(\"review_mirror_controller_worktree_not_clean\")\n+    if require_current_origin_main and _origin_main_sha(repo_root) != expected_sha:\n+        raise ImplementationError(\"review_mirror_stale_origin_main_sha\")\n+    if mirror_root.name != expected_sha or mirror_root.is_symlink() or not mirror_root.is_dir():\n+        raise ImplementationError(\"review_mirror_root_invalid\")\n+    manifest_path = mirror_root / \"reviewed-bundle-manifest.v1.json\"\n+    if manifest_path.is_symlink() or not manifest_path.is_file():\n+        raise ImplementationError(\"review_mirror_manifest_missing_or_symlink\")\n+    manifest_bytes = manifest_path.read_bytes()\n+    manifest = json.loads(manifest_bytes)\n+    if not isinstance(manifest, dict) or manifest.get(\"source_origin_main_sha\") != expected_sha:\n+        raise ImplementationError(\"review_mirror_manifest_source_sha_mismatch\")\n+\n+    expected_artifacts: list[dict[str, Any]] = []\n+    for relative in REVIEWED_IMPLEMENTATION_PATHS:\n+        source_bytes, git_mode, git_object_id = _git_blob(repo_root, expected_sha, relative)\n+        mirrored_path = mirror_root / \"files\" / relative\n+        if mirrored_path.is_symlink() or not mirrored_path.is_file():\n+            raise ImplementationError(f\"review_mirror_file_missing:{relative}\")\n+        mirrored_bytes = mirrored_path.read_bytes()\n+        if mirrored_bytes != source_bytes:\n+            raise ImplementationError(f\"review_mirror_file_hash_mismatch:{relative}\")\n+        metadata = os.lstat(mirrored_path)\n+        if require_root_ownership and (metadata.st_uid != 0 or metadata.st_gid != 0):\n+            raise ImplementationError(f\"review_mirror_not_root_owned:{relative}\")\n+        if stat.S_IMODE(metadata.st_mode) & 0o222:\n+            raise ImplementationError(f\"review_mirror_file_writable:{relative}\")\n+        expected_artifacts.append(\n+            {\n+                \"path\": relative,\n+                \"sha256\": sha256_bytes(source_bytes),\n+                \"byte_length\": len(source_bytes),\n+                \"git_mode\": git_mode,\n+                \"git_object_id\": git_object_id,\n+            }\n+        )\n+    if manifest.get(\"reviewed_artifacts\") != expected_artifacts:\n+        raise ImplementationError(\"review_mirror_manifest_artifact_inventory_mismatch\")\n+    hash_entries = [\n+        {\"path\": item[\"path\"], \"sha256\": item[\"sha256\"]}\n+        for item in expected_artifacts\n+    ]\n+    bundle_hash = reviewed_bundle_hash(hash_entries)\n+    if manifest.get(\"reviewed_bundle_hash\") != bundle_hash:\n+        raise ImplementationError(\"review_mirror_bundle_hash_mismatch\")\n+    observed = set(_scan_exact_mirror_files(mirror_root))\n+    expected = _mirror_expected_relative_files()\n+    if observed != expected:\n+        raise ImplementationError(\n+            \"review_mirror_exact_set_mismatch:\"\n+            f\"missing={sorted(expected-observed)}:extra={sorted(observed-expected)}\"\n+        )\n+    source_sha_path = mirror_root / \"source-origin-main-sha\"\n+    if source_sha_path.read_bytes() != (expected_sha + \"\\n\").encode(\"ascii\"):\n+        raise ImplementationError(\"review_mirror_source_sha_bytes_mismatch\")\n+    expected_inventory = {\n+        \"schema_version\": \"parent_creation_review_inventory.v1\",\n+        \"source_origin_main_sha\": expected_sha,\n+        \"paths\": list(REVIEWED_IMPLEMENTATION_PATHS),\n+        \"path_count\": len(REVIEWED_IMPLEMENTATION_PATHS),\n+        \"inventory_hash\": canonical_sha256(list(REVIEWED_IMPLEMENTATION_PATHS)),\n+    }\n+    inventory_path = mirror_root / \"reviewed-inventory.v1.json\"\n+    inventory_bytes = inventory_path.read_bytes()\n+    if json.loads(inventory_bytes) != expected_inventory:\n+        raise ImplementationError(\"review_mirror_inventory_mismatch\")\n+    producer_acl_evidence_only = manifest.get(\"producer_acl_evidence_only\")\n+    if not isinstance(producer_acl_evidence_only, bool):\n+        raise ImplementationError(\"review_mirror_producer_acl_evidence_flag_invalid\")\n+    if controller_head_sha != expected_sha and not producer_acl_evidence_only:\n+        raise ImplementationError(\"review_mirror_hybrid_controller_not_explicitly_scoped\")\n+    binding_eligible = (\n+        controller_worktree_clean\n+        and controller_head_sha == expected_sha\n+        and not producer_acl_evidence_only\n+    )\n+    if manifest.get(\"independent_review_binding_eligible\") is not binding_eligible:\n+        raise ImplementationError(\"review_mirror_binding_eligibility_mismatch\")\n+    expected_manifest = {\n+        \"schema_version\": \"parent_creation_review_bundle_manifest.v1\",\n+        \"source_ref\": \"origin/main\",\n+        \"controller_repo_head_sha\": controller_head_sha,\n+        \"controller_worktree_clean\": controller_worktree_clean,\n+        \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+        \"independent_review_binding_eligible\": binding_eligible,\n+        \"source_origin_main_sha\": expected_sha,\n+        \"reviewed_path_count\": len(expected_artifacts),\n+        \"reviewed_bundle_hash\": bundle_hash,\n+        \"reviewed_artifacts\": expected_artifacts,\n+    }\n+    if manifest != expected_manifest:\n+        raise ImplementationError(\"review_mirror_manifest_mismatch\")\n+    materialization_path = mirror_root / \"materialization-receipt.v1.json\"\n+    materialization_bytes = materialization_path.read_bytes()\n+    materialization = json.loads(materialization_bytes)\n+    required_materialization = {\n+        \"schema_version\": \"parent_creation_review_mirror_materialization_receipt.v1\",\n+        \"materializer_effective_uid\": 0 if require_root_ownership else os.geteuid(),\n+        \"source_ref\": \"origin/main\",\n+        \"controller_repo_head_sha\": controller_head_sha,\n+        \"controller_worktree_clean\": controller_worktree_clean,\n+        \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+        \"independent_review_binding_eligible\": binding_eligible,\n+        \"source_origin_main_sha_before\": expected_sha,\n+        \"source_origin_main_sha\": expected_sha,\n+        \"source_origin_main_sha_after\": expected_sha,\n+        \"mirror_root\": str(mirror_root),\n+        \"reviewed_path_count\": len(expected_artifacts),\n+        \"reviewed_inventory_hash\": expected_inventory[\"inventory_hash\"],\n+        \"reviewed_bundle_hash\": bundle_hash,\n+        \"source_sha_file_sha256\": sha256_bytes(source_sha_path.read_bytes()),\n+        \"reviewed_inventory_file_sha256\": sha256_bytes(inventory_bytes),\n+        \"reviewed_manifest_file_sha256\": sha256_bytes(manifest_bytes),\n+        \"root_owned\": require_root_ownership,\n+        \"atomic_staging_publish\": True,\n+        \"files_fsynced\": True,\n+        \"path_traversal_rejected\": True,\n+        \"symlinks_rejected\": True,\n+        \"exact_set_required\": True,\n+        \"exact_set_verified\": True,\n+        \"hashes_verified\": True,\n+        \"symlink_scan_passed\": True,\n+        \"path_traversal_scan_passed\": True,\n+        \"extra_file_scan_passed\": True,\n+        \"producer_can_modify_after_materialization\": False,\n+        \"validation_results\": {\n+            \"source_sha_exact\": True,\n+            \"inventory_exact\": True,\n+            \"per_path_git_bytes_exact\": True,\n+            \"bundle_hash_recomputed\": True,\n+            \"missing_extra_symlink_rejected\": True,\n+        },\n+        \"does_not_constitute_independent_review_receipt\": True,\n+        \"provider_calls\": 0,\n+        \"remote_data_writes\": 0,\n+        \"campaign_a_started\": False,\n+    }\n+    if not isinstance(materialization, dict):\n+        raise ImplementationError(\"review_mirror_materialization_receipt_invalid\")\n+    for field, expected_value in required_materialization.items():\n+        if materialization.get(field) != expected_value:\n+            raise ImplementationError(\n+                f\"review_mirror_materialization_receipt_field_mismatch:{field}\"\n+            )\n+    timestamp = materialization.get(\"materialized_at\")\n+    try:\n+        parsed_timestamp = datetime.fromisoformat(\n+            timestamp[:-1] + \"+00:00\"\n+            if isinstance(timestamp, str) and timestamp.endswith(\"Z\")\n+            else str(timestamp)\n+        )\n+    except ValueError:\n+        parsed_timestamp = None\n+    if (\n+        not isinstance(timestamp, str)\n+        or not timestamp.endswith(\"Z\")\n+        or parsed_timestamp is None\n+        or parsed_timestamp.tzinfo is None\n+    ):\n+        raise ImplementationError(\"review_mirror_materialization_timestamp_invalid\")\n+    for path in (mirror_root, *mirror_root.rglob(\"*\")):\n+        metadata = os.lstat(path)\n+        if require_root_ownership and (metadata.st_uid, metadata.st_gid) != (0, 0):\n+            raise ImplementationError(f\"review_mirror_not_root_owned:{path}\")\n+        expected_mode = 0o550 if stat.S_ISDIR(metadata.st_mode) else 0o440\n+        if stat.S_IMODE(metadata.st_mode) != expected_mode:\n+            raise ImplementationError(f\"review_mirror_mode_mismatch:{path}\")\n+    return {\n+        \"schema_version\": \"parent_creation_review_mirror_readback.v1\",\n+        \"source_origin_main_sha\": expected_sha,\n+        \"mirror_root\": str(mirror_root),\n+        \"reviewed_path_count\": len(expected_artifacts),\n+        \"reviewed_bundle_hash\": bundle_hash,\n+        \"materialization_receipt_sha256\": sha256_bytes(materialization_bytes),\n+        \"exact_set_verified\": True,\n+        \"hashes_verified\": True,\n+        \"symlink_count\": 0,\n+        \"materialization_passed\": True,\n+        \"does_not_constitute_independent_review_receipt\": True,\n+    }\n+\n+\n+def materialize_review_mirror(\n+    *,\n+    repo_root: Path,\n+    durable_root: Path,\n+    expected_sha: str | None = None,\n+    require_root: bool = True,\n+    producer_acl_evidence_only: bool = False,\n+) -> dict[str, Any]:\n+    \"\"\"Materialize reviewed Git blobs into an immutable exact-SHA staging tree.\"\"\"\n+    if require_root:\n+        ensure_root()\n+    controller_head_sha = _git_run(repo_root, \"rev-parse\", \"HEAD\").decode().strip()\n+    controller_worktree_clean = not bool(\n+        _git_run(repo_root, \"status\", \"--porcelain\").strip()\n+    )\n+    if require_root and not controller_worktree_clean:\n+        raise ImplementationError(\"review_mirror_controller_worktree_not_clean\")\n+    source_sha_before = _origin_main_sha(repo_root)\n+    if expected_sha is not None and source_sha_before != expected_sha:\n+        raise ImplementationError(\"requested_review_sha_is_not_current_origin_main\")\n+    source_sha = expected_sha or source_sha_before\n+    if controller_head_sha != source_sha and not producer_acl_evidence_only:\n+        raise ImplementationError(\n+            \"review_mirror_controller_head_must_equal_source_sha_unless_producer_acl_evidence_only\"\n+        )\n+    mirror_parent = durable_root / REVIEW_INPUT_RELATIVE_ROOT\n+    if mirror_parent.is_symlink():\n+        raise ImplementationError(\"review_mirror_parent_symlink_forbidden\")\n+    mirror_parent.mkdir(parents=True, exist_ok=True)\n+    final_root = mirror_parent / source_sha\n+    if final_root.exists() or final_root.is_symlink():\n+        raise ImplementationError(\"review_mirror_final_path_already_exists\")\n+    staging = Path(tempfile.mkdtemp(prefix=f\".{source_sha}.\", dir=mirror_parent))\n+    if require_root:\n+        os.chown(staging, 0, 0)\n+    try:\n+        artifacts: list[dict[str, Any]] = []\n+        for relative in REVIEWED_IMPLEMENTATION_PATHS:\n+            source_bytes, git_mode, git_object_id = _git_blob(repo_root, source_sha, relative)\n+            target = staging / \"files\" / relative\n+            _write_new_file(target, source_bytes, mode=0o440, root_owned=require_root)\n+            artifacts.append(\n+                {\n+                    \"path\": relative,\n+                    \"sha256\": sha256_bytes(source_bytes),\n+                    \"byte_length\": len(source_bytes),\n+                    \"git_mode\": git_mode,\n+                    \"git_object_id\": git_object_id,\n+                }\n+            )\n+        hash_entries = [\n+            {\"path\": item[\"path\"], \"sha256\": item[\"sha256\"]} for item in artifacts\n+        ]\n+        bundle_hash = reviewed_bundle_hash(hash_entries)\n+        inventory = {\n+            \"schema_version\": \"parent_creation_review_inventory.v1\",\n+            \"source_origin_main_sha\": source_sha,\n+            \"paths\": list(REVIEWED_IMPLEMENTATION_PATHS),\n+            \"path_count\": len(REVIEWED_IMPLEMENTATION_PATHS),\n+            \"inventory_hash\": canonical_sha256(list(REVIEWED_IMPLEMENTATION_PATHS)),\n+        }\n+        manifest = {\n+            \"schema_version\": \"parent_creation_review_bundle_manifest.v1\",\n+            \"source_ref\": \"origin/main\",\n+            \"controller_repo_head_sha\": controller_head_sha,\n+            \"controller_worktree_clean\": controller_worktree_clean,\n+            \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+            \"independent_review_binding_eligible\": (\n+                controller_worktree_clean\n+                and controller_head_sha == source_sha\n+                and not producer_acl_evidence_only\n+            ),\n+            \"source_origin_main_sha\": source_sha,\n+            \"reviewed_path_count\": len(artifacts),\n+            \"reviewed_bundle_hash\": bundle_hash,\n+            \"reviewed_artifacts\": artifacts,\n+        }\n+        source_sha_payload = (source_sha + \"\\n\").encode(\"ascii\")\n+        inventory_payload = (\n+            json.dumps(inventory, ensure_ascii=False, indent=2) + \"\\n\"\n+        ).encode()\n+        manifest_payload = (\n+            json.dumps(manifest, ensure_ascii=False, indent=2) + \"\\n\"\n+        ).encode()\n+        _write_new_file(\n+            staging / \"source-origin-main-sha\",\n+            source_sha_payload,\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        _write_new_file(\n+            staging / \"reviewed-inventory.v1.json\",\n+            inventory_payload,\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        _write_new_file(\n+            staging / \"reviewed-bundle-manifest.v1.json\",\n+            manifest_payload,\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        source_sha_after = _origin_main_sha(repo_root)\n+        if source_sha_after != source_sha_before:\n+            raise ImplementationError(\"origin_main_changed_during_review_materialization\")\n+        receipt = {\n+            \"schema_version\": \"parent_creation_review_mirror_materialization_receipt.v1\",\n+            \"materialized_at\": utc_now(),\n+            \"materializer_effective_uid\": os.geteuid(),\n+            \"source_ref\": \"origin/main\",\n+            \"controller_repo_head_sha\": controller_head_sha,\n+            \"controller_worktree_clean\": controller_worktree_clean,\n+            \"producer_acl_evidence_only\": producer_acl_evidence_only,\n+            \"independent_review_binding_eligible\": (\n+                controller_worktree_clean\n+                and controller_head_sha == source_sha\n+                and not producer_acl_evidence_only\n+            ),\n+            \"source_origin_main_sha_before\": source_sha_before,\n+            \"source_origin_main_sha\": source_sha,\n+            \"source_origin_main_sha_after\": source_sha_after,\n+            \"mirror_root\": str(final_root),\n+            \"reviewed_path_count\": len(artifacts),\n+            \"reviewed_inventory_hash\": inventory[\"inventory_hash\"],\n+            \"reviewed_bundle_hash\": bundle_hash,\n+            \"source_sha_file_sha256\": sha256_bytes(source_sha_payload),\n+            \"reviewed_inventory_file_sha256\": sha256_bytes(inventory_payload),\n+            \"reviewed_manifest_file_sha256\": sha256_bytes(manifest_payload),\n+            \"root_owned\": require_root,\n+            \"atomic_staging_publish\": True,\n+            \"files_fsynced\": True,\n+            \"path_traversal_rejected\": True,\n+            \"symlinks_rejected\": True,\n+            \"exact_set_required\": True,\n+            \"exact_set_verified\": True,\n+            \"hashes_verified\": True,\n+            \"symlink_scan_passed\": True,\n+            \"path_traversal_scan_passed\": True,\n+            \"extra_file_scan_passed\": True,\n+            \"producer_can_modify_after_materialization\": False,\n+            \"validation_results\": {\n+                \"source_sha_exact\": True,\n+                \"inventory_exact\": True,\n+                \"per_path_git_bytes_exact\": True,\n+                \"bundle_hash_recomputed\": True,\n+                \"missing_extra_symlink_rejected\": True,\n+            },\n+            \"does_not_constitute_independent_review_receipt\": True,\n+            \"provider_calls\": 0,\n+            \"remote_data_writes\": 0,\n+            \"campaign_a_started\": False,\n+        }\n+        _write_new_file(\n+            staging / \"materialization-receipt.v1.json\",\n+            (json.dumps(receipt, ensure_ascii=False, indent=2) + \"\\n\").encode(),\n+            mode=0o440,\n+            root_owned=require_root,\n+        )\n+        for path in sorted(staging.rglob(\"*\"), reverse=True):\n+            if path.is_dir():\n+                if require_root:\n+                    os.chown(path, 0, 0)\n+                os.chmod(path, 0o550)\n+        os.chmod(staging, 0o550)\n+        _fsync_directory(staging)\n+        os.replace(staging, final_root)\n+        _fsync_directory(mirror_parent)\n+        validate_review_mirror(\n+            final_root,\n+            repo_root=repo_root,\n+            expected_sha=source_sha,\n+            require_current_origin_main=True,\n+            require_root_ownership=require_root,\n+        )\n+        return receipt\n+    except Exception:\n+        # Preserve partial evidence; never delete or overwrite it.\n+        if staging.exists():\n+            quarantine = mirror_parent / f\"failed-{staging.name.removeprefix('.')}\"\n+            if not quarantine.exists():\n+                os.replace(staging, quarantine)\n+        raise\n+\n+\n def ensure_root() -> None:\n     if os.geteuid() != 0:\n         raise ImplementationError(\"root_required\")\n \n \n+def reviewer_remediation_plan(\n+    *, durable_root: Path = Path(DURABLE_ROOT), state_root: Path = Path(STATE_ROOT)\n+) -> dict[str, Any]:\n+    return {\n+        \"schema_version\": \"cie9709_parent_creation_reviewer_remediation_plan.v1\",\n+        \"durable_root\": str(durable_root),\n+        \"state_root\": str(state_root),\n+        \"review_input_root\": str(durable_root / REVIEW_INPUT_RELATIVE_ROOT),\n+        \"reviewer_observation_root\": str(\n+            durable_root / \"role-inbox\" / REVIEWER_ROLE_ID\n+        ),\n+        \"reviewer_checkpoint_root\": str(\n+            durable_root / \"checkpoints\" / REVIEWER_ROLE_ID\n+        ),\n+        \"acl_grants_to_remove\": [\n+            str(durable_root / relative)\n+            for relative in REVIEWER_ACL_REMOVE_RELATIVE_PATHS\n+        ],\n+        \"managed_scope_acl_enforcement\": {\n+            \"existing_unlisted_directories\": \"named_reviewer_access_and_default_acl_none\",\n+            \"required_traverse_only\": [\n+                str(durable_root),\n+                str(durable_root / \"review-inputs\"),\n+                str(durable_root / \"role-inbox\"),\n+                str(durable_root / \"checkpoints\"),\n+            ],\n+            \"review_input_subtree\": \"named_reviewer_read_execute_no_write\",\n+            \"own_observation_and_checkpoint\": \"reviewer_owner_0700_default_owner_only\",\n+            \"other_principals_permissions_unchanged\": True,\n+        },\n+        \"ledger_broker_runtime_remediation\": {\n+            \"contract\": ledger_broker_runtime_contract(),\n+            \"source_unit\": str(ROOT / LEDGER_SOCKET_UNIT_REPO_PATH),\n+            \"runtime_unit\": LEDGER_SOCKET_UNIT_RUNTIME_PATH,\n+            \"pre_change_unit_archived\": True,\n+            \"atomic_unit_replace\": True,\n+            \"daemon_reload_and_socket_restart\": True,\n+            \"runtime_directory_snapshot_separate_from_reviewer_managed_scope\": True,\n+        },\n+        \"normalized_reviewer_access\": reviewer_access_contract(),\n+        \"incremental_only\": True,\n+        \"pristine_apply_invoked\": False,\n+        \"destructive_delete_available\": False,\n+        \"rollback_strategy\": \"restore_exact_acl_snapshot_and_quarantine_new_paths\",\n+    }\n+\n+\n+def _metadata_receipt(path: Path) -> dict[str, Any]:\n+    if not path.exists() and not path.is_symlink():\n+        return {\"path\": str(path), \"state\": \"absent\"}\n+    metadata = os.lstat(path)\n+    return {\n+        \"path\": str(path),\n+        \"state\": \"present\",\n+        \"uid\": metadata.st_uid,\n+        \"gid\": metadata.st_gid,\n+        \"mode\": f\"{stat.S_IMODE(metadata.st_mode):04o}\",\n+        \"is_directory\": stat.S_ISDIR(metadata.st_mode),\n+        \"is_symlink\": stat.S_ISLNK(metadata.st_mode),\n+    }\n+\n+\n+def _reviewer_touched_paths(durable_root: Path) -> list[Path]:\n+    paths = [\n+        durable_root / \"review-inputs\",\n+        durable_root / REVIEW_INPUT_RELATIVE_ROOT,\n+        durable_root / REVIEWER_OBSERVATION_RELATIVE_ROOT,\n+        durable_root / REVIEWER_CHECKPOINT_RELATIVE_ROOT,\n+        *(durable_root / relative for relative in REVIEWER_ACL_REMOVE_RELATIVE_PATHS),\n+    ]\n+    for current, directories, _ in os.walk(durable_root, followlinks=False):\n+        current_path = Path(current)\n+        if current_path.is_symlink():\n+            directories[:] = []\n+            continue\n+        paths.append(current_path)\n+        directories[:] = [\n+            name for name in directories if not (current_path / name).is_symlink()\n+        ]\n+    return sorted(set(paths))\n+\n+\n+def _reviewer_remediation_snapshot_paths(durable_root: Path) -> list[Path]:\n+    \"\"\"Include runtime trust paths without applying reviewer ACLs outside /srv.\"\"\"\n+    return sorted(\n+        {\n+            *_reviewer_touched_paths(durable_root),\n+            Path(RUN_ROOT),\n+            Path(LEDGER_SOCKET_UNIT_RUNTIME_PATH),\n+        }\n+    )\n+\n+\n+def _apply_reviewer_managed_scope_default_deny(durable_root: Path) -> dict[str, Any]:\n+    \"\"\"Give UID 977 only the exact traversals and directories in its contract.\"\"\"\n+    reviewer = pwd.getpwnam(REVIEWER_USER)\n+    review_inputs = durable_root / \"review-inputs\"\n+    review_parent = durable_root / REVIEW_INPUT_RELATIVE_ROOT\n+    observation_parent = durable_root / \"role-inbox\"\n+    checkpoint_parent = durable_root / \"checkpoints\"\n+    own_roots = {\n+        durable_root / REVIEWER_OBSERVATION_RELATIVE_ROOT,\n+        durable_root / REVIEWER_CHECKPOINT_RELATIVE_ROOT,\n+    }\n+    traverse_only = {\n+        durable_root,\n+        review_inputs,\n+        observation_parent,\n+        checkpoint_parent,\n+    }\n+    denied: list[str] = []\n+    traversed: list[str] = []\n+    reviewed: list[str] = []\n+    owned: list[str] = []\n+    for path in _reviewer_touched_paths(durable_root):\n+        if not path.exists():\n+            continue\n+        if path.is_symlink() or not path.is_dir():\n+            raise ImplementationError(f\"reviewer_managed_scope_path_unsafe:{path}\")\n+        if path in own_roots or any(root in path.parents for root in own_roots):\n+            if path in own_roots:\n+                normalize_reviewer_output_acl(path)\n+            owned.append(str(path))\n+            continue\n+        if path == review_parent or review_parent in path.parents:\n+            run([\"setfacl\", \"-m\", f\"u:{REVIEWER_USER}:rx\", str(path)])\n+            run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:rx\", str(path)])\n+            reviewed.append(str(path))\n+            continue\n+        if path in traverse_only:\n+            run([\"setfacl\", \"-m\", f\"u:{REVIEWER_USER}:x\", str(path)])\n+            traversed.append(str(path))\n+            continue\n+        run([\"setfacl\", \"-m\", f\"u:{REVIEWER_USER}:---\", str(path)])\n+        run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:---\", str(path)])\n+        denied.append(str(path))\n+\n+    # Make future direct children fail closed for this principal.  Existing\n+    # default ACL base entries are preserved by setfacl; owner semantics on the\n+    # reviewer's own child directories still take precedence over named-user ACLs.\n+    for path in (durable_root, review_inputs, observation_parent, checkpoint_parent):\n+        run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:---\", str(path)])\n+    run([\"setfacl\", \"-d\", \"-m\", f\"u:{REVIEWER_USER}:rx\", str(review_parent)])\n+    for path in own_roots:\n+        metadata = os.lstat(path)\n+        if (metadata.st_uid, metadata.st_gid) != (reviewer.pw_uid, reviewer.pw_gid):\n+            raise ImplementationError(f\"reviewer_output_owner_mismatch:{path}\")\n+        normalize_reviewer_output_acl(path)\n+    return {\n+        \"explicit_deny_paths\": denied,\n+        \"traverse_only_paths\": traversed,\n+        \"review_mirror_read_paths\": reviewed,\n+        \"owner_only_output_paths\": owned,\n+        \"unlisted_managed_directories_default_deny\": True,\n+    }\n+\n+\n+def _capture_acl_snapshot(paths: list[Path]) -> bytes:\n+    existing = [str(path) for path in paths if path.exists() and not path.is_symlink()]\n+    if not existing:\n+        return b\"\"\n+    return run([\"getfacl\", \"-p\", *existing]).stdout\n+\n+\n+def _archive_prior_file(path: Path, receipt_root: Path) -> dict[str, Any]:\n+    if path.is_symlink() or not path.is_file():\n+        raise ImplementationError(f\"superseded_runtime_file_invalid:{path}\")\n+    payload = path.read_bytes()\n+    target = receipt_root / f\"prior-{path.name}\"\n+    _write_new_file(target, payload, mode=0o600, root_owned=True)\n+    return {\n+        \"runtime_path\": str(path),\n+        \"archive_path\": str(target),\n+        \"prior_sha256\": sha256_bytes(payload),\n+        \"prior_byte_length\": len(payload),\n+    }\n+\n+\n+def normalize_reviewer_output_acl(path: Path) -> None:\n+    \"\"\"Conserve owner-only access/default ACLs without widening mode via a mask.\"\"\"\n+    run([\"setfacl\", \"-b\", str(path)])\n+    run([\"setfacl\", \"-k\", str(path)])\n+    run(\n+        [\n+            \"setfacl\",\n+            \"-d\",\n+            \"-m\",\n+            \"u::rwx,g::---,o::---\",\n+            str(path),\n+        ]\n+    )\n+    os.chmod(path, 0o700)\n+    if stat.S_IMODE(os.lstat(path).st_mode) != 0o700:\n+        raise ImplementationError(f\"reviewer_output_mode_not_0700:{path}\")\n+\n+\n+def apply_reviewer_remediation(\n+    *, durable_root: Path = Path(DURABLE_ROOT), state_root: Path = Path(STATE_ROOT)\n+) -> dict[str, Any]:\n+    \"\"\"Incrementally repair only the exact-SHA reviewer ACL surface.\"\"\"\n+    ensure_root()\n+    if durable_root.is_symlink() or not durable_root.is_dir():\n+        raise ImplementationError(\"reviewer_remediation_durable_root_invalid\")\n+    controller_head_sha = _git_run(ROOT, \"rev-parse\", \"HEAD\").decode().strip()\n+    controller_origin_main_sha = _origin_main_sha(ROOT)\n+    if _git_run(ROOT, \"status\", \"--porcelain\").strip():\n+        raise ImplementationError(\"reviewer_remediation_controller_worktree_not_clean\")\n+    _assert_ledger_runtime_root_safe_for_adoption()\n+    source_socket_unit = ROOT / LEDGER_SOCKET_UNIT_REPO_PATH\n+    runtime_socket_unit = Path(LEDGER_SOCKET_UNIT_RUNTIME_PATH)\n+    for path in (source_socket_unit, runtime_socket_unit):\n+        if path.is_symlink() or not path.is_file():\n+            raise ImplementationError(f\"ledger_socket_unit_unsafe:{path}\")","path":"scripts/vlm/implement_cie9709_host_v1.py","commit_id":"b3c4b9e773b015bb9f2eb6c86a82a76d55306b21","original_commit_id":"29d8eb7b0670f6dffe9c15f59aaa40b41c8e91d2","user":{"login":"Samsen879","id":172257175,"node_id":"U_kgDOCkRvlw","avatar_url":"https://avatars.githubusercontent.com/u/172257175?v=4","gravatar_id":"","url":"https://api.github.com/users/Samsen879","html_url":"https://github.com/Samsen879","followers_url":"https://api.github.com/users/Samsen879/followers","following_url":"https://api.github.com/users/Samsen879/following{/other_user}","gists_url":"https://api.github.com/users/Samsen879/gists{/gist_id}","starred_url":"https://api.github.com/users/Samsen879/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/Samsen879/subscriptions","organizations_url":"https://api.github.com/users/Samsen879/orgs","repos_url":"https://api.github.com/users/Samsen879/repos","events_url":"https://api.github.com/users/Samsen879/events{/privacy}","received_events_url":"https://api.github.com/users/Samsen879/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Checked the current committed tree and diff: these validators already have one canonical definition. `reviewer_access_contract`, `_reviewer_policy_grants`, `_reviewer_managed_scope_acl_scan_errors`, and `reviewer_access_conservation_errors` are defined only in `scripts/vlm/cie9709_host_contract_v1.py`; `implement_cie9709_host_v1.py` imports `reviewer_access_contract` and does not define any of the four. The large anchored hunk in the implementation file is the distinct root-only mirror/materialization and ACL mutation path, not a duplicate copy of the conservation validators. `rg` confirms the exact single-definition set, so no code change is warranted here.","created_at":"2026-07-13T16:02:57Z","updated_at":"2026-07-13T16:02:57Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1546#discussion_r3572170505","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1546","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3572170505"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1546#discussion_r3572170505"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1546"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3572170505/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":490,"original_start_line":490,"start_side":"RIGHT","line":1096,"original_line":1081,"side":"RIGHT","in_reply_to_id":3572129662,"author_association":"OWNER","original_position":1002,"position":1017,"subject_type":"line"}]