[{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753252","pull_request_review_id":4757470513,"id":3632753252,"node_id":"PRRC_kwDOO5D6p87Yh2pk","diff_hunk":"@@ -0,0 +1,695 @@\n+#!/usr/bin/env python3\n+\"\"\"Import, validate, compare, and prepare resolution for Batch005 checkers.\"\"\"\n+from __future__ import annotations\n+\n+from collections import Counter\n+import argparse\n+import hashlib\n+import json\n+from pathlib import Path\n+from typing import Any\n+\n+from jsonschema import Draft202012Validator\n+\n+\n+REPO = Path(__file__).resolve().parents[2]\n+ISSUE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-structural-batch-005-1670\"\n+FREEZE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-production-freeze-1650\"\n+EXTERNAL_ROOT = Path.home() / \".local/share/cie9709/full-corpus/9709-structural-batch-005\"\n+BATCH_ID = \"9709-structural-batch-005\"\n+PROTOCOL_SHA256 = \"fba7d4e4d5a12574cdb78fe1a9c3fe30c0c3fd03ae717dde4c5caa81b54fd2ab\"\n+PRIMARY = {\n+    \"checker-shard-01\": (\"ao-checker-01\", 77),\n+    \"checker-shard-02\": (\"ao-checker-02\", 74),\n+    \"checker-shard-03\": (\"ao-checker-03\", 76),\n+}\n+SECOND = {\n+    \"checker-shard-01\": \"ao-checker-01-second-source-read\",\n+    \"checker-shard-02\": \"ao-checker-02b\",\n+    \"checker-shard-03\": \"ao-checker-03b\",\n+}\n+FORBIDDEN_KEYS = {\n+    \"maker_candidate\", \"maker_output\", \"maker_warnings\", \"expected_structure\",\n+    \"historical_gold\", \"gold\", \"truth\", \"other_checker_outputs\", \"resolver_output\",\n+}\n+\n+\n+def canonical_bytes(value: Any) -> bytes:\n+    return json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\")).encode()\n+\n+\n+def frozen_serialize_bytes(value: Any) -> bytes:\n+    \"\"\"Match the frozen JS `serialize`: two-space JSON plus trailing newline.\"\"\"\n+    return (json.dumps(value, ensure_ascii=False, indent=2, separators=(\",\", \": \")) + \"\\n\").encode()\n+\n+\n+def sha256_bytes(value: bytes) -> str:\n+    return hashlib.sha256(value).hexdigest()\n+\n+\n+def sha256_file(path: Path) -> str:\n+    return sha256_bytes(path.read_bytes())\n+\n+\n+def read_json(path: Path) -> Any:\n+    return json.loads(path.read_text(encoding=\"utf-8\"))\n+\n+\n+def write_once(path: Path, value: Any) -> None:\n+    payload = canonical_bytes(value) + b\"\\n\"\n+    if path.exists():\n+        if path.read_bytes() != payload:\n+            raise RuntimeError(f\"write_once_mismatch:{path}\")\n+        return\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    with path.open(\"xb\") as handle:\n+        handle.write(payload)\n+\n+\n+def record_sha256(value: dict[str, Any]) -> str:\n+    payload = json.loads(json.dumps(value))\n+    del payload[\"provenance\"][\"record_sha256\"]\n+    return sha256_bytes(canonical_bytes(payload))\n+\n+\n+def recursive_keys(value: Any) -> set[str]:\n+    if isinstance(value, dict):\n+        return set(value) | set().union(*(recursive_keys(item) for item in value.values()))\n+    if isinstance(value, list):\n+        return set().union(*(recursive_keys(item) for item in value)) if value else set()\n+    return set()\n+\n+\n+def extraction_validator() -> Draft202012Validator:\n+    schema = read_json(FREEZE_ROOT / \"production-objects.schema.v1.json\")\n+    return Draft202012Validator({\n+        \"$schema\": schema[\"$schema\"], \"$defs\": schema[\"$defs\"],\n+        \"$ref\": \"#/$defs/extraction_output\",\n+    })\n+\n+\n+def topology_errors(value: dict[str, Any]) -> list[str]:\n+    nodes = value[\"structural_nodes\"]\n+    paths = [node[\"owner_path\"] for node in nodes]\n+    errors = []\n+    if len(paths) != len(set(paths)):\n+        errors.append(\"duplicate_owner_path\")\n+    owner_set = set(paths)\n+    root = value[\"question_id\"].rsplit(\"_q\", 1)[1]\n+    for node in nodes:\n+        segments = node[\"owner_path\"].split(\"/\")\n+        expected_parent = \"/\".join(segments[:-1]) if len(segments) > 1 else None\n+        expected_label = str(int(root)) if len(segments) == 1 else segments[-1]\n+        observed_label = str(node[\"printed_label\"]).strip().removeprefix(\"(\").removesuffix(\")\").lower()\n+        if segments[0] != f\"q{root}\": errors.append(f\"{node['owner_path']}:question_root\")\n+        if node[\"parent_owner_path\"] != expected_parent: errors.append(f\"{node['owner_path']}:parent\")\n+        if expected_parent is not None and expected_parent not in owner_set: errors.append(f\"{node['owner_path']}:orphan\")\n+        if node[\"whole_question_alias\"] != node[\"owner_path\"].endswith(\"/$whole\"): errors.append(f\"{node['owner_path']}:whole_alias\")\n+        if \"$whole\" in segments[:-1]: errors.append(f\"{node['owner_path']}:whole_descendant\")\n+        if observed_label != expected_label.lower(): errors.append(f\"{node['owner_path']}:printed_label\")\n+    return errors\n+\n+\n+def evidence_row_key(row: dict[str, Any]) -> tuple:\n+    return (\n+        row.get(\"source_object_id\"), row.get(\"source_sha256\"), row.get(\"page_number\"),\n+        row.get(\"page_image_sha256\"), row.get(\"crop_sha256\"),\n+    )\n+\n+\n+def terminal_summary(root: Path) -> Path:\n+    rows = sorted(root.glob(\"terminal*.json\"))\n+    if len(rows) > 1 and all(path.name.startswith(\"terminal-receipt.v\") for path in rows):\n+        return rows[-1]\n+    if len(rows) != 1:\n+        raise RuntimeError(f\"terminal_summary_membership:{root}:{len(rows)}\")\n+    return rows[0]\n+\n+\n+def record_files(root: Path) -> list[Path]:\n+    return sorted(path for path in (root / \"records\").rglob(\"*.json\") if path.is_file())\n+\n+\n+def validate_lane(\n+    *, shard_id: str, worker_id: str, expected_ids: set[str], root: Path,\n+    packet_by_id: dict[str, dict], bundle_by_id: dict[str, dict], validator: Draft202012Validator,\n+    official_inventory_sha256: str, source_inventory_sha256: str,\n+    allow_expected_superset: bool = False,\n+) -> dict[str, Any]:\n+    summary_path = terminal_summary(root)\n+    summary = read_json(summary_path)\n+    if (\n+        summary.get(\"candidate_blind\") is not True\n+        or summary.get(\"candidate_responses_used\") is True\n+        or summary.get(\"shard_id\") != shard_id\n+        or summary.get(\"worker_id\") != worker_id\n+    ):\n+        raise RuntimeError(f\"terminal_summary_invalid:{shard_id}:{worker_id}\")\n+    files = record_files(root)\n+    values = [read_json(path) for path in files]\n+    ids = [row.get(\"question_id\") for row in values]\n+    membership_valid = set(ids) >= expected_ids if allow_expected_superset else set(ids) == expected_ids\n+    if len(ids) != len(set(ids)) or not membership_valid:\n+        raise RuntimeError(f\"lane_membership:{shard_id}:{len(ids)}:{len(expected_ids)}\")","path":"scripts/vlm/issue_1670_reconcile.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"chatgpt-codex-connector[bot]","id":199175422,"node_id":"BOT_kgDOC98s_g","avatar_url":"https://avatars.githubusercontent.com/in/1144995?v=4","gravatar_id":"","url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D","html_url":"https://github.com/apps/chatgpt-codex-connector","followers_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/followers","following_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/following{/other_user}","gists_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/gists{/gist_id}","starred_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/subscriptions","organizations_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/orgs","repos_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/repos","events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/events{/privacy}","received_events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/received_events","type":"Bot","user_view_type":"public","site_admin":false},"body":"**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Require globally exact second-read membership**\n\nWhen the 48 additional second reads contain a duplicate question from another shard while omitting a different low-risk question, each lane still satisfies this superset check and the aggregate count can remain 227. `prepare_resolver` then collapses duplicate IDs into `second_rows` without checking its membership and treats an omitted row as having no second read, allowing maker/primary agreement alone to reach `verified_agreement`. Validate that the union of second-read IDs is exactly the 227-question packet membership and that IDs are globally disjoint across lanes.\n\nUseful? React with 👍 / 👎.","created_at":"2026-07-22T18:23:46Z","updated_at":"2026-07-22T18:23:46Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753252","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753252"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753252"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753252/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":151,"original_start_line":151,"start_side":"RIGHT","line":153,"original_line":153,"side":"RIGHT","author_association":"NONE","original_position":153,"position":153,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753255","pull_request_review_id":4757470513,"id":3632753255,"node_id":"PRRC_kwDOO5D6p87Yh2pn","diff_hunk":"@@ -0,0 +1,695 @@\n+#!/usr/bin/env python3\n+\"\"\"Import, validate, compare, and prepare resolution for Batch005 checkers.\"\"\"\n+from __future__ import annotations\n+\n+from collections import Counter\n+import argparse\n+import hashlib\n+import json\n+from pathlib import Path\n+from typing import Any\n+\n+from jsonschema import Draft202012Validator\n+\n+\n+REPO = Path(__file__).resolve().parents[2]\n+ISSUE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-structural-batch-005-1670\"\n+FREEZE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-production-freeze-1650\"\n+EXTERNAL_ROOT = Path.home() / \".local/share/cie9709/full-corpus/9709-structural-batch-005\"\n+BATCH_ID = \"9709-structural-batch-005\"\n+PROTOCOL_SHA256 = \"fba7d4e4d5a12574cdb78fe1a9c3fe30c0c3fd03ae717dde4c5caa81b54fd2ab\"\n+PRIMARY = {\n+    \"checker-shard-01\": (\"ao-checker-01\", 77),\n+    \"checker-shard-02\": (\"ao-checker-02\", 74),\n+    \"checker-shard-03\": (\"ao-checker-03\", 76),\n+}\n+SECOND = {\n+    \"checker-shard-01\": \"ao-checker-01-second-source-read\",\n+    \"checker-shard-02\": \"ao-checker-02b\",\n+    \"checker-shard-03\": \"ao-checker-03b\",\n+}\n+FORBIDDEN_KEYS = {\n+    \"maker_candidate\", \"maker_output\", \"maker_warnings\", \"expected_structure\",\n+    \"historical_gold\", \"gold\", \"truth\", \"other_checker_outputs\", \"resolver_output\",\n+}\n+\n+\n+def canonical_bytes(value: Any) -> bytes:\n+    return json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\")).encode()\n+\n+\n+def frozen_serialize_bytes(value: Any) -> bytes:\n+    \"\"\"Match the frozen JS `serialize`: two-space JSON plus trailing newline.\"\"\"\n+    return (json.dumps(value, ensure_ascii=False, indent=2, separators=(\",\", \": \")) + \"\\n\").encode()\n+\n+\n+def sha256_bytes(value: bytes) -> str:\n+    return hashlib.sha256(value).hexdigest()\n+\n+\n+def sha256_file(path: Path) -> str:\n+    return sha256_bytes(path.read_bytes())\n+\n+\n+def read_json(path: Path) -> Any:\n+    return json.loads(path.read_text(encoding=\"utf-8\"))\n+\n+\n+def write_once(path: Path, value: Any) -> None:\n+    payload = canonical_bytes(value) + b\"\\n\"\n+    if path.exists():\n+        if path.read_bytes() != payload:\n+            raise RuntimeError(f\"write_once_mismatch:{path}\")\n+        return\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    with path.open(\"xb\") as handle:\n+        handle.write(payload)\n+\n+\n+def record_sha256(value: dict[str, Any]) -> str:\n+    payload = json.loads(json.dumps(value))\n+    del payload[\"provenance\"][\"record_sha256\"]\n+    return sha256_bytes(canonical_bytes(payload))\n+\n+\n+def recursive_keys(value: Any) -> set[str]:\n+    if isinstance(value, dict):\n+        return set(value) | set().union(*(recursive_keys(item) for item in value.values()))\n+    if isinstance(value, list):\n+        return set().union(*(recursive_keys(item) for item in value)) if value else set()\n+    return set()\n+\n+\n+def extraction_validator() -> Draft202012Validator:\n+    schema = read_json(FREEZE_ROOT / \"production-objects.schema.v1.json\")\n+    return Draft202012Validator({\n+        \"$schema\": schema[\"$schema\"], \"$defs\": schema[\"$defs\"],\n+        \"$ref\": \"#/$defs/extraction_output\",\n+    })\n+\n+\n+def topology_errors(value: dict[str, Any]) -> list[str]:\n+    nodes = value[\"structural_nodes\"]\n+    paths = [node[\"owner_path\"] for node in nodes]\n+    errors = []\n+    if len(paths) != len(set(paths)):\n+        errors.append(\"duplicate_owner_path\")\n+    owner_set = set(paths)\n+    root = value[\"question_id\"].rsplit(\"_q\", 1)[1]\n+    for node in nodes:\n+        segments = node[\"owner_path\"].split(\"/\")\n+        expected_parent = \"/\".join(segments[:-1]) if len(segments) > 1 else None\n+        expected_label = str(int(root)) if len(segments) == 1 else segments[-1]\n+        observed_label = str(node[\"printed_label\"]).strip().removeprefix(\"(\").removesuffix(\")\").lower()\n+        if segments[0] != f\"q{root}\": errors.append(f\"{node['owner_path']}:question_root\")\n+        if node[\"parent_owner_path\"] != expected_parent: errors.append(f\"{node['owner_path']}:parent\")\n+        if expected_parent is not None and expected_parent not in owner_set: errors.append(f\"{node['owner_path']}:orphan\")\n+        if node[\"whole_question_alias\"] != node[\"owner_path\"].endswith(\"/$whole\"): errors.append(f\"{node['owner_path']}:whole_alias\")\n+        if \"$whole\" in segments[:-1]: errors.append(f\"{node['owner_path']}:whole_descendant\")\n+        if observed_label != expected_label.lower(): errors.append(f\"{node['owner_path']}:printed_label\")\n+    return errors\n+\n+\n+def evidence_row_key(row: dict[str, Any]) -> tuple:\n+    return (\n+        row.get(\"source_object_id\"), row.get(\"source_sha256\"), row.get(\"page_number\"),\n+        row.get(\"page_image_sha256\"), row.get(\"crop_sha256\"),\n+    )\n+\n+\n+def terminal_summary(root: Path) -> Path:\n+    rows = sorted(root.glob(\"terminal*.json\"))\n+    if len(rows) > 1 and all(path.name.startswith(\"terminal-receipt.v\") for path in rows):\n+        return rows[-1]\n+    if len(rows) != 1:\n+        raise RuntimeError(f\"terminal_summary_membership:{root}:{len(rows)}\")\n+    return rows[0]\n+\n+\n+def record_files(root: Path) -> list[Path]:\n+    return sorted(path for path in (root / \"records\").rglob(\"*.json\") if path.is_file())\n+\n+\n+def validate_lane(\n+    *, shard_id: str, worker_id: str, expected_ids: set[str], root: Path,\n+    packet_by_id: dict[str, dict], bundle_by_id: dict[str, dict], validator: Draft202012Validator,\n+    official_inventory_sha256: str, source_inventory_sha256: str,\n+    allow_expected_superset: bool = False,\n+) -> dict[str, Any]:\n+    summary_path = terminal_summary(root)\n+    summary = read_json(summary_path)\n+    if (\n+        summary.get(\"candidate_blind\") is not True\n+        or summary.get(\"candidate_responses_used\") is True\n+        or summary.get(\"shard_id\") != shard_id\n+        or summary.get(\"worker_id\") != worker_id\n+    ):\n+        raise RuntimeError(f\"terminal_summary_invalid:{shard_id}:{worker_id}\")\n+    files = record_files(root)\n+    values = [read_json(path) for path in files]\n+    ids = [row.get(\"question_id\") for row in values]\n+    membership_valid = set(ids) >= expected_ids if allow_expected_superset else set(ids) == expected_ids\n+    if len(ids) != len(set(ids)) or not membership_valid:\n+        raise RuntimeError(f\"lane_membership:{shard_id}:{len(ids)}:{len(expected_ids)}\")\n+    rows = []\n+    dialects = Counter()\n+    for path, value in zip(files, values, strict=True):\n+        question_id = value[\"question_id\"]\n+        packet_row = packet_by_id[question_id]\n+        bundle_row = bundle_by_id[question_id]\n+        packet = read_json(Path(packet_row[\"path\"]))\n+        bundle = read_json(Path(bundle_row[\"path\"]))\n+        evidence_path = Path(bundle[\"evidence_manifest_path\"])\n+        evidence = read_json(evidence_path)\n+        evidence_hash = sha256_bytes(frozen_serialize_bytes({\"schema_version\": evidence[\"schema_version\"], \"rows\": evidence[\"rows\"]}))\n+        errors = sorted(validator.iter_errors(value), key=lambda error: list(error.path))\n+        if errors:\n+            raise RuntimeError(f\"schema_invalid:{question_id}:{errors[0].json_path}:{errors[0].message}\")\n+        relational_errors = topology_errors(value)\n+        if recursive_keys(value) & FORBIDDEN_KEYS:\n+            raise RuntimeError(f\"candidate_blindness_invalid:{question_id}\")\n+        producer_record_hash = value[\"provenance\"][\"record_sha256\"]\n+        if producer_record_hash == record_sha256(value):\n+            record_hash_dialect = \"canonical_record_without_record_sha256\"\n+        elif isinstance(producer_record_hash, str) and len(producer_record_hash) == 64 and all(\n+            character in \"0123456789abcdef\" for character in producer_record_hash\n+        ):\n+            record_hash_dialect = \"opaque_producer_hash_with_integration_file_sha256\"\n+        else:\n+            raise RuntimeError(f\"record_hash_shape_invalid:{question_id}\")\n+        dialects[record_hash_dialect] += 1\n+        accepted_shard_ids = {shard_id}\n+        if allow_expected_superset:\n+            accepted_shard_ids.add(f\"{shard_id}-second-read\")\n+        provenance_shard_id = value[\"provenance\"][\"shard_id\"]\n+        dialects[\n+            \"second_read_shard_suffix\" if provenance_shard_id != shard_id\n+            else \"canonical_shard_id\"\n+        ] += 1\n+        if (\n+            sha256_file(Path(packet_row[\"path\"])) != packet_row[\"sha256\"]\n+            or sha256_file(Path(bundle_row[\"path\"])) != bundle_row[\"sha256\"]\n+            or evidence_hash != evidence[\"manifest_sha256\"] != \"\"\n+            or evidence_hash != bundle[\"evidence_manifest_sha256\"]\n+            or value[\"question\"][\"question_id\"] != question_id\n+            or value[\"question\"][\"paper_id\"] != packet[\"paper_id\"]\n+            or value[\"question\"][\"verification_state\"] != \"checker_complete\"\n+            or value[\"question\"][\"complete_page_chain\"] != [row[\"page_number\"] for row in bundle[\"qp_complete_page_chain\"]]\n+            or value[\"question\"][\"source_membership\"] != [packet[\"source_object_ids\"][\"qp\"], packet[\"source_object_ids\"][\"ms\"]]\n+            or value[\"provenance\"][\"worker_id\"] != worker_id\n+            or provenance_shard_id not in accepted_shard_ids\n+            or value[\"provenance\"][\"protocol_sha256\"] != PROTOCOL_SHA256\n+        ):\n+            raise RuntimeError(f\"identity_or_authority_invalid:{question_id}\")\n+        inventory_value = value[\"question\"][\"provenance\"][\"inventory_sha256\"]\n+        if inventory_value == official_inventory_sha256:\n+            dialects[\"official_inventory_file_sha256\"] += 1\n+        elif inventory_value == summary.get(\"inventory_sha256\"):\n+            dialects[\"producer_terminal_inventory_sha256\"] += 1\n+        else:\n+            raise RuntimeError(f\"inventory_authority_unbound:{question_id}\")\n+        input_value = value[\"provenance\"][\"input_manifest_sha256\"]\n+        input_dialects = {\n+            evidence_hash: \"evidence_manifest_sha256\",\n+            packet_row[\"sha256\"]: \"canonical_packet_file_sha256\",\n+            bundle_row[\"sha256\"]: \"visual_bundle_file_sha256\",\n+            bundle.get(\"canonical_packet_sha256\"): \"canonical_packet_file_sha256\",\n+            source_inventory_sha256: \"source_inventory_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-packet-manifest.v5.json\"):\n+                \"checker_packet_manifest_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\"):\n+                \"checker_visual_bundle_manifest_file_sha256\",\n+        }\n+        if input_value not in input_dialects:\n+            raise RuntimeError(f\"input_authority_unbound:{question_id}:{input_value}\")\n+        dialects[input_dialects[input_value]] += 1\n+        official_rows = {evidence_row_key(row) for row in evidence[\"rows\"]}\n+        claimed_rows = [*value[\"question\"][\"authoritative_regions\"]]\n+        for node in value[\"structural_nodes\"]:\n+            claimed_rows.extend(node[\"page_region_evidence\"])\n+            claimed_rows.extend(node[\"ms_evidence\"])\n+        if any(evidence_row_key(row) not in official_rows for row in claimed_rows):\n+            raise RuntimeError(f\"source_evidence_unbound:{question_id}\")\n+        findings = sorted({\n+            warning.split(\":\", 1)[1] for warning in value.get(\"warnings\", [])\n+            if warning.startswith(\"structural_relational_finding:\")\n+        })\n+        rows.append({\n+            \"question_id\": question_id, \"paper_id\": value[\"question\"][\"paper_id\"],\n+            \"path\": str(path), \"file_sha256\": sha256_file(path),\n+            \"record_sha256\": producer_record_hash, \"record_hash_dialect\": record_hash_dialect,\n+            \"input_authority_sha256\": input_value,\n+            \"input_authority_dialect\": input_dialects[input_value],\n+            \"inventory_authority_sha256\": inventory_value,\n+            \"resolver_required_relational_findings\": findings,\n+        })\n+    rows.sort(key=lambda row: row[\"question_id\"])\n+    return {\n+        \"shard_id\": shard_id, \"worker_id\": worker_id, \"question_count\": len(rows),\n+        \"root\": str(root), \"terminal_summary\": {\"path\": str(summary_path), \"sha256\": sha256_file(summary_path)},\n+        \"record_set_sha256\": sha256_bytes(canonical_bytes([[row[\"question_id\"], row[\"file_sha256\"]] for row in rows])),\n+        \"authority_dialect_counts\": dict(sorted(dialects.items())), \"records\": rows,\n+    }\n+\n+\n+def import_checkers(_args: argparse.Namespace) -> dict[str, Any]:\n+    packet_manifest = read_json(ISSUE_ROOT / \"checker-packet-manifest.v5.json\")\n+    bundle_manifest = read_json(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\")\n+    packet_by_id = {row[\"question_id\"]: row for row in packet_manifest[\"packets\"]}\n+    bundle_by_id = {row[\"question_id\"]: row for row in bundle_manifest[\"bundles\"]}\n+    if len(packet_by_id) != len(bundle_by_id) != 0 or set(packet_by_id) != set(bundle_by_id):\n+        raise RuntimeError(\"packet_bundle_membership_invalid\")\n+    official_inventory_sha256 = sha256_file(FREEZE_ROOT / \"inventory.v1.json\")\n+    source_inventory_sha256 = sha256_file(ISSUE_ROOT / \"source-evidence-inventory.v2.json\")\n+    validator = extraction_validator()\n+    expected_by_shard = {\n+        shard_id: {row[\"question_id\"] for row in packet_manifest[\"packets\"] if row[\"shard_id\"] == shard_id}\n+        for shard_id in PRIMARY\n+    }\n+    high_risk_by_shard = {shard_id: set() for shard_id in PRIMARY}\n+    for row in bundle_manifest[\"bundles\"]:\n+        bundle = read_json(Path(row[\"path\"]))\n+        if bundle[\"layout_risk_triggers\"] != [\"ordinary_single_page\"]:\n+            high_risk_by_shard[row[\"shard_id\"]].add(row[\"question_id\"])\n+    primary = []\n+    second = []\n+    for shard_id, (worker_id, expected) in PRIMARY.items():\n+        if len(expected_by_shard[shard_id]) != expected:\n+            raise RuntimeError(f\"primary_expected_count:{shard_id}\")\n+        primary.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=expected_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-outputs-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+        ))\n+    for shard_id, worker_id in SECOND.items():\n+        second.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=high_risk_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-second-reads-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+            allow_expected_superset=True,\n+        ))\n+    primary_count = sum(row[\"question_count\"] for row in primary)\n+    second_count = sum(row[\"question_count\"] for row in second)\n+    high_risk_counts = {shard_id: len(rows) for shard_id, rows in high_risk_by_shard.items()}\n+    if primary_count != 227 or second_count != 227 or sum(high_risk_counts.values()) != 179:\n+        raise RuntimeError(f\"checker_conservation:{primary_count}:{second_count}:{high_risk_counts}\")\n+    all_records = [record for lane in [*primary, *second] for record in lane[\"records\"]]\n+    finding_records = [row for row in all_records if row[\"resolver_required_relational_findings\"]]\n+    primary_receipt = {\n+        \"schema_version\": \"9709_batch_005_primary_checker_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"question_count\": primary_count, \"paper_count\": 27, \"shards\": primary,\n+    }\n+    second_receipt = {\n+        \"schema_version\": \"9709_batch_005_independent_second_read_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"primary_outputs_read_by_second_readers\": False,\n+        \"source_authoritative_high_risk_count\": 179, \"second_read_count\": second_count,\n+        \"additional_risk_stratified_sample_read_count\": second_count - 179,\n+        \"shard_counts\": high_risk_counts, \"shards\": second,\n+    }\n+    existing_import_path = ISSUE_ROOT / \"checker-import-and-validation-receipt.v1.json\"\n+    frozen_generator = read_json(existing_import_path)[\"generator\"] if existing_import_path.exists() else {\"path\": str(Path(__file__).relative_to(REPO)), \"sha256\": sha256_file(Path(__file__))}","path":"scripts/vlm/issue_1670_reconcile.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"chatgpt-codex-connector[bot]","id":199175422,"node_id":"BOT_kgDOC98s_g","avatar_url":"https://avatars.githubusercontent.com/in/1144995?v=4","gravatar_id":"","url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D","html_url":"https://github.com/apps/chatgpt-codex-connector","followers_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/followers","following_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/following{/other_user}","gists_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/gists{/gist_id}","starred_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/subscriptions","organizations_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/orgs","repos_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/repos","events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/events{/privacy}","received_events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/received_events","type":"Bot","user_view_type":"public","site_admin":false},"body":"**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Reject stale checker-validator provenance**\n\nWhen this receipt already exists, preserving its old `generator` object allows the validation to pass after the referenced script changes. That has happened in this commit: the committed receipt names `scripts/vlm/issue_1670_reconcile.py` with SHA-256 `4c25b3…`, while that committed file hashes to `e14fd6…`, so the exact validator that allegedly produced the checker evidence is unavailable and replays silently retain invalid provenance. Require the recorded hash to match the current file or publish a new immutable receipt tied to the new generator.\n\nUseful? React with 👍 / 👎.","created_at":"2026-07-22T18:23:46Z","updated_at":"2026-07-22T18:23:46Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753255","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753255"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753255"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753255/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":323,"original_start_line":313,"start_side":"RIGHT","line":324,"original_line":314,"side":"RIGHT","author_association":"NONE","original_position":314,"position":324,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753258","pull_request_review_id":4757470513,"id":3632753258,"node_id":"PRRC_kwDOO5D6p87Yh2pq","diff_hunk":"@@ -0,0 +1,695 @@\n+#!/usr/bin/env python3\n+\"\"\"Import, validate, compare, and prepare resolution for Batch005 checkers.\"\"\"\n+from __future__ import annotations\n+\n+from collections import Counter\n+import argparse\n+import hashlib\n+import json\n+from pathlib import Path\n+from typing import Any\n+\n+from jsonschema import Draft202012Validator\n+\n+\n+REPO = Path(__file__).resolve().parents[2]\n+ISSUE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-structural-batch-005-1670\"\n+FREEZE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-production-freeze-1650\"\n+EXTERNAL_ROOT = Path.home() / \".local/share/cie9709/full-corpus/9709-structural-batch-005\"\n+BATCH_ID = \"9709-structural-batch-005\"\n+PROTOCOL_SHA256 = \"fba7d4e4d5a12574cdb78fe1a9c3fe30c0c3fd03ae717dde4c5caa81b54fd2ab\"\n+PRIMARY = {\n+    \"checker-shard-01\": (\"ao-checker-01\", 77),\n+    \"checker-shard-02\": (\"ao-checker-02\", 74),\n+    \"checker-shard-03\": (\"ao-checker-03\", 76),\n+}\n+SECOND = {\n+    \"checker-shard-01\": \"ao-checker-01-second-source-read\",\n+    \"checker-shard-02\": \"ao-checker-02b\",\n+    \"checker-shard-03\": \"ao-checker-03b\",\n+}\n+FORBIDDEN_KEYS = {\n+    \"maker_candidate\", \"maker_output\", \"maker_warnings\", \"expected_structure\",\n+    \"historical_gold\", \"gold\", \"truth\", \"other_checker_outputs\", \"resolver_output\",\n+}\n+\n+\n+def canonical_bytes(value: Any) -> bytes:\n+    return json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\")).encode()\n+\n+\n+def frozen_serialize_bytes(value: Any) -> bytes:\n+    \"\"\"Match the frozen JS `serialize`: two-space JSON plus trailing newline.\"\"\"\n+    return (json.dumps(value, ensure_ascii=False, indent=2, separators=(\",\", \": \")) + \"\\n\").encode()\n+\n+\n+def sha256_bytes(value: bytes) -> str:\n+    return hashlib.sha256(value).hexdigest()\n+\n+\n+def sha256_file(path: Path) -> str:\n+    return sha256_bytes(path.read_bytes())\n+\n+\n+def read_json(path: Path) -> Any:\n+    return json.loads(path.read_text(encoding=\"utf-8\"))\n+\n+\n+def write_once(path: Path, value: Any) -> None:\n+    payload = canonical_bytes(value) + b\"\\n\"\n+    if path.exists():\n+        if path.read_bytes() != payload:\n+            raise RuntimeError(f\"write_once_mismatch:{path}\")\n+        return\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    with path.open(\"xb\") as handle:\n+        handle.write(payload)\n+\n+\n+def record_sha256(value: dict[str, Any]) -> str:\n+    payload = json.loads(json.dumps(value))\n+    del payload[\"provenance\"][\"record_sha256\"]\n+    return sha256_bytes(canonical_bytes(payload))\n+\n+\n+def recursive_keys(value: Any) -> set[str]:\n+    if isinstance(value, dict):\n+        return set(value) | set().union(*(recursive_keys(item) for item in value.values()))\n+    if isinstance(value, list):\n+        return set().union(*(recursive_keys(item) for item in value)) if value else set()\n+    return set()\n+\n+\n+def extraction_validator() -> Draft202012Validator:\n+    schema = read_json(FREEZE_ROOT / \"production-objects.schema.v1.json\")\n+    return Draft202012Validator({\n+        \"$schema\": schema[\"$schema\"], \"$defs\": schema[\"$defs\"],\n+        \"$ref\": \"#/$defs/extraction_output\",\n+    })\n+\n+\n+def topology_errors(value: dict[str, Any]) -> list[str]:\n+    nodes = value[\"structural_nodes\"]\n+    paths = [node[\"owner_path\"] for node in nodes]\n+    errors = []\n+    if len(paths) != len(set(paths)):\n+        errors.append(\"duplicate_owner_path\")\n+    owner_set = set(paths)\n+    root = value[\"question_id\"].rsplit(\"_q\", 1)[1]\n+    for node in nodes:\n+        segments = node[\"owner_path\"].split(\"/\")\n+        expected_parent = \"/\".join(segments[:-1]) if len(segments) > 1 else None\n+        expected_label = str(int(root)) if len(segments) == 1 else segments[-1]\n+        observed_label = str(node[\"printed_label\"]).strip().removeprefix(\"(\").removesuffix(\")\").lower()\n+        if segments[0] != f\"q{root}\": errors.append(f\"{node['owner_path']}:question_root\")\n+        if node[\"parent_owner_path\"] != expected_parent: errors.append(f\"{node['owner_path']}:parent\")\n+        if expected_parent is not None and expected_parent not in owner_set: errors.append(f\"{node['owner_path']}:orphan\")\n+        if node[\"whole_question_alias\"] != node[\"owner_path\"].endswith(\"/$whole\"): errors.append(f\"{node['owner_path']}:whole_alias\")\n+        if \"$whole\" in segments[:-1]: errors.append(f\"{node['owner_path']}:whole_descendant\")\n+        if observed_label != expected_label.lower(): errors.append(f\"{node['owner_path']}:printed_label\")\n+    return errors\n+\n+\n+def evidence_row_key(row: dict[str, Any]) -> tuple:\n+    return (\n+        row.get(\"source_object_id\"), row.get(\"source_sha256\"), row.get(\"page_number\"),\n+        row.get(\"page_image_sha256\"), row.get(\"crop_sha256\"),\n+    )\n+\n+\n+def terminal_summary(root: Path) -> Path:\n+    rows = sorted(root.glob(\"terminal*.json\"))\n+    if len(rows) > 1 and all(path.name.startswith(\"terminal-receipt.v\") for path in rows):\n+        return rows[-1]\n+    if len(rows) != 1:\n+        raise RuntimeError(f\"terminal_summary_membership:{root}:{len(rows)}\")\n+    return rows[0]\n+\n+\n+def record_files(root: Path) -> list[Path]:\n+    return sorted(path for path in (root / \"records\").rglob(\"*.json\") if path.is_file())\n+\n+\n+def validate_lane(\n+    *, shard_id: str, worker_id: str, expected_ids: set[str], root: Path,\n+    packet_by_id: dict[str, dict], bundle_by_id: dict[str, dict], validator: Draft202012Validator,\n+    official_inventory_sha256: str, source_inventory_sha256: str,\n+    allow_expected_superset: bool = False,\n+) -> dict[str, Any]:\n+    summary_path = terminal_summary(root)\n+    summary = read_json(summary_path)\n+    if (\n+        summary.get(\"candidate_blind\") is not True\n+        or summary.get(\"candidate_responses_used\") is True\n+        or summary.get(\"shard_id\") != shard_id\n+        or summary.get(\"worker_id\") != worker_id\n+    ):\n+        raise RuntimeError(f\"terminal_summary_invalid:{shard_id}:{worker_id}\")\n+    files = record_files(root)\n+    values = [read_json(path) for path in files]\n+    ids = [row.get(\"question_id\") for row in values]\n+    membership_valid = set(ids) >= expected_ids if allow_expected_superset else set(ids) == expected_ids\n+    if len(ids) != len(set(ids)) or not membership_valid:\n+        raise RuntimeError(f\"lane_membership:{shard_id}:{len(ids)}:{len(expected_ids)}\")\n+    rows = []\n+    dialects = Counter()\n+    for path, value in zip(files, values, strict=True):\n+        question_id = value[\"question_id\"]\n+        packet_row = packet_by_id[question_id]\n+        bundle_row = bundle_by_id[question_id]\n+        packet = read_json(Path(packet_row[\"path\"]))\n+        bundle = read_json(Path(bundle_row[\"path\"]))\n+        evidence_path = Path(bundle[\"evidence_manifest_path\"])\n+        evidence = read_json(evidence_path)\n+        evidence_hash = sha256_bytes(frozen_serialize_bytes({\"schema_version\": evidence[\"schema_version\"], \"rows\": evidence[\"rows\"]}))\n+        errors = sorted(validator.iter_errors(value), key=lambda error: list(error.path))\n+        if errors:\n+            raise RuntimeError(f\"schema_invalid:{question_id}:{errors[0].json_path}:{errors[0].message}\")\n+        relational_errors = topology_errors(value)\n+        if recursive_keys(value) & FORBIDDEN_KEYS:\n+            raise RuntimeError(f\"candidate_blindness_invalid:{question_id}\")\n+        producer_record_hash = value[\"provenance\"][\"record_sha256\"]\n+        if producer_record_hash == record_sha256(value):\n+            record_hash_dialect = \"canonical_record_without_record_sha256\"\n+        elif isinstance(producer_record_hash, str) and len(producer_record_hash) == 64 and all(\n+            character in \"0123456789abcdef\" for character in producer_record_hash\n+        ):\n+            record_hash_dialect = \"opaque_producer_hash_with_integration_file_sha256\"\n+        else:\n+            raise RuntimeError(f\"record_hash_shape_invalid:{question_id}\")\n+        dialects[record_hash_dialect] += 1\n+        accepted_shard_ids = {shard_id}\n+        if allow_expected_superset:\n+            accepted_shard_ids.add(f\"{shard_id}-second-read\")\n+        provenance_shard_id = value[\"provenance\"][\"shard_id\"]\n+        dialects[\n+            \"second_read_shard_suffix\" if provenance_shard_id != shard_id\n+            else \"canonical_shard_id\"\n+        ] += 1\n+        if (\n+            sha256_file(Path(packet_row[\"path\"])) != packet_row[\"sha256\"]\n+            or sha256_file(Path(bundle_row[\"path\"])) != bundle_row[\"sha256\"]\n+            or evidence_hash != evidence[\"manifest_sha256\"] != \"\"\n+            or evidence_hash != bundle[\"evidence_manifest_sha256\"]\n+            or value[\"question\"][\"question_id\"] != question_id\n+            or value[\"question\"][\"paper_id\"] != packet[\"paper_id\"]\n+            or value[\"question\"][\"verification_state\"] != \"checker_complete\"\n+            or value[\"question\"][\"complete_page_chain\"] != [row[\"page_number\"] for row in bundle[\"qp_complete_page_chain\"]]\n+            or value[\"question\"][\"source_membership\"] != [packet[\"source_object_ids\"][\"qp\"], packet[\"source_object_ids\"][\"ms\"]]\n+            or value[\"provenance\"][\"worker_id\"] != worker_id\n+            or provenance_shard_id not in accepted_shard_ids\n+            or value[\"provenance\"][\"protocol_sha256\"] != PROTOCOL_SHA256\n+        ):\n+            raise RuntimeError(f\"identity_or_authority_invalid:{question_id}\")\n+        inventory_value = value[\"question\"][\"provenance\"][\"inventory_sha256\"]\n+        if inventory_value == official_inventory_sha256:\n+            dialects[\"official_inventory_file_sha256\"] += 1\n+        elif inventory_value == summary.get(\"inventory_sha256\"):\n+            dialects[\"producer_terminal_inventory_sha256\"] += 1\n+        else:\n+            raise RuntimeError(f\"inventory_authority_unbound:{question_id}\")\n+        input_value = value[\"provenance\"][\"input_manifest_sha256\"]\n+        input_dialects = {\n+            evidence_hash: \"evidence_manifest_sha256\",\n+            packet_row[\"sha256\"]: \"canonical_packet_file_sha256\",\n+            bundle_row[\"sha256\"]: \"visual_bundle_file_sha256\",\n+            bundle.get(\"canonical_packet_sha256\"): \"canonical_packet_file_sha256\",\n+            source_inventory_sha256: \"source_inventory_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-packet-manifest.v5.json\"):\n+                \"checker_packet_manifest_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\"):\n+                \"checker_visual_bundle_manifest_file_sha256\",\n+        }\n+        if input_value not in input_dialects:\n+            raise RuntimeError(f\"input_authority_unbound:{question_id}:{input_value}\")\n+        dialects[input_dialects[input_value]] += 1\n+        official_rows = {evidence_row_key(row) for row in evidence[\"rows\"]}\n+        claimed_rows = [*value[\"question\"][\"authoritative_regions\"]]\n+        for node in value[\"structural_nodes\"]:\n+            claimed_rows.extend(node[\"page_region_evidence\"])\n+            claimed_rows.extend(node[\"ms_evidence\"])\n+        if any(evidence_row_key(row) not in official_rows for row in claimed_rows):\n+            raise RuntimeError(f\"source_evidence_unbound:{question_id}\")\n+        findings = sorted({\n+            warning.split(\":\", 1)[1] for warning in value.get(\"warnings\", [])\n+            if warning.startswith(\"structural_relational_finding:\")\n+        })\n+        rows.append({\n+            \"question_id\": question_id, \"paper_id\": value[\"question\"][\"paper_id\"],\n+            \"path\": str(path), \"file_sha256\": sha256_file(path),\n+            \"record_sha256\": producer_record_hash, \"record_hash_dialect\": record_hash_dialect,\n+            \"input_authority_sha256\": input_value,\n+            \"input_authority_dialect\": input_dialects[input_value],\n+            \"inventory_authority_sha256\": inventory_value,\n+            \"resolver_required_relational_findings\": findings,\n+        })\n+    rows.sort(key=lambda row: row[\"question_id\"])\n+    return {\n+        \"shard_id\": shard_id, \"worker_id\": worker_id, \"question_count\": len(rows),\n+        \"root\": str(root), \"terminal_summary\": {\"path\": str(summary_path), \"sha256\": sha256_file(summary_path)},\n+        \"record_set_sha256\": sha256_bytes(canonical_bytes([[row[\"question_id\"], row[\"file_sha256\"]] for row in rows])),\n+        \"authority_dialect_counts\": dict(sorted(dialects.items())), \"records\": rows,\n+    }\n+\n+\n+def import_checkers(_args: argparse.Namespace) -> dict[str, Any]:\n+    packet_manifest = read_json(ISSUE_ROOT / \"checker-packet-manifest.v5.json\")\n+    bundle_manifest = read_json(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\")\n+    packet_by_id = {row[\"question_id\"]: row for row in packet_manifest[\"packets\"]}\n+    bundle_by_id = {row[\"question_id\"]: row for row in bundle_manifest[\"bundles\"]}\n+    if len(packet_by_id) != len(bundle_by_id) != 0 or set(packet_by_id) != set(bundle_by_id):\n+        raise RuntimeError(\"packet_bundle_membership_invalid\")\n+    official_inventory_sha256 = sha256_file(FREEZE_ROOT / \"inventory.v1.json\")\n+    source_inventory_sha256 = sha256_file(ISSUE_ROOT / \"source-evidence-inventory.v2.json\")\n+    validator = extraction_validator()\n+    expected_by_shard = {\n+        shard_id: {row[\"question_id\"] for row in packet_manifest[\"packets\"] if row[\"shard_id\"] == shard_id}\n+        for shard_id in PRIMARY\n+    }\n+    high_risk_by_shard = {shard_id: set() for shard_id in PRIMARY}\n+    for row in bundle_manifest[\"bundles\"]:\n+        bundle = read_json(Path(row[\"path\"]))\n+        if bundle[\"layout_risk_triggers\"] != [\"ordinary_single_page\"]:\n+            high_risk_by_shard[row[\"shard_id\"]].add(row[\"question_id\"])\n+    primary = []\n+    second = []\n+    for shard_id, (worker_id, expected) in PRIMARY.items():\n+        if len(expected_by_shard[shard_id]) != expected:\n+            raise RuntimeError(f\"primary_expected_count:{shard_id}\")\n+        primary.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=expected_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-outputs-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+        ))\n+    for shard_id, worker_id in SECOND.items():\n+        second.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=high_risk_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-second-reads-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+            allow_expected_superset=True,\n+        ))\n+    primary_count = sum(row[\"question_count\"] for row in primary)\n+    second_count = sum(row[\"question_count\"] for row in second)\n+    high_risk_counts = {shard_id: len(rows) for shard_id, rows in high_risk_by_shard.items()}\n+    if primary_count != 227 or second_count != 227 or sum(high_risk_counts.values()) != 179:\n+        raise RuntimeError(f\"checker_conservation:{primary_count}:{second_count}:{high_risk_counts}\")\n+    all_records = [record for lane in [*primary, *second] for record in lane[\"records\"]]\n+    finding_records = [row for row in all_records if row[\"resolver_required_relational_findings\"]]\n+    primary_receipt = {\n+        \"schema_version\": \"9709_batch_005_primary_checker_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"question_count\": primary_count, \"paper_count\": 27, \"shards\": primary,\n+    }\n+    second_receipt = {\n+        \"schema_version\": \"9709_batch_005_independent_second_read_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"primary_outputs_read_by_second_readers\": False,\n+        \"source_authoritative_high_risk_count\": 179, \"second_read_count\": second_count,\n+        \"additional_risk_stratified_sample_read_count\": second_count - 179,\n+        \"shard_counts\": high_risk_counts, \"shards\": second,\n+    }\n+    existing_import_path = ISSUE_ROOT / \"checker-import-and-validation-receipt.v1.json\"\n+    frozen_generator = read_json(existing_import_path)[\"generator\"] if existing_import_path.exists() else {\"path\": str(Path(__file__).relative_to(REPO)), \"sha256\": sha256_file(Path(__file__))}\n+    import_receipt = {\n+        \"schema_version\": \"9709_batch_005_checker_import_and_validation_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"generator\": frozen_generator,\n+        \"packet_manifest_sha256\": sha256_file(ISSUE_ROOT / \"checker-packet-manifest.v5.json\"),\n+        \"bundle_manifest_sha256\": sha256_file(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\"),\n+        \"primary_question_count\": primary_count, \"second_read_question_count\": second_count,\n+        \"schema_valid_count\": primary_count + second_count,\n+        \"source_evidence_identity_provenance_valid_count\": primary_count + second_count,\n+        \"resolver_required_relational_finding_record_count\": len(finding_records),\n+        \"resolver_required_relational_finding_question_count\": len({row[\"question_id\"] for row in finding_records}),\n+        \"candidate_blind_count\": primary_count + second_count, \"validation_error_count\": 0,\n+        \"checker_output_bytes_rewritten\": False, \"provider_calls_during_import\": 0,\n+        \"completed_checker_reads_repeated\": 0,\n+    }\n+    write_once(ISSUE_ROOT / \"checker-primary-custody.v1.json\", primary_receipt)\n+    write_once(ISSUE_ROOT / \"checker-second-read-custody.v1.json\", second_receipt)\n+    write_once(ISSUE_ROOT / \"checker-import-and-validation-receipt.v1.json\", import_receipt)\n+    topology_rows = []\n+    for lane_name, lanes in ((\"primary\", primary), (\"second_read\", second)):\n+        for lane in lanes:\n+            for record in lane[\"records\"]:\n+                errors = topology_errors(read_json(Path(record[\"path\"])))\n+                if errors:\n+                    topology_rows.append({\"lane\": lane_name, \"question_id\": record[\"question_id\"], \"errors\": errors,\n+                        \"final_state\": None})\n+    pre_path = ISSUE_ROOT / \"pre-resolution-verification-ledger.v1.json\"\n+    if pre_path.exists():\n+        states = {row[\"question_id\"]: row[\"state\"] for row in read_json(pre_path)[\"rows\"]}\n+        for row in topology_rows:\n+            row[\"final_state\"] = states[row[\"question_id\"]]\n+    topology_receipt = {\n+        \"schema_version\": \"9709_structural_batch_005_checker_topology_validation_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"primary_schema_valid_count\": primary_count,\n+        \"second_read_schema_valid_count\": second_count, \"topology_finding_record_count\": len(topology_rows),\n+        \"verified_agreement_with_topology_finding_count\": sum(row[\"final_state\"] == \"verified_agreement\" for row in topology_rows),\n+        \"topology_findings_routed_to_disagreement_or_safe_rejection\": all(row[\"final_state\"] in {None, \"checker_disagreement\", \"safe_rejection\"} for row in topology_rows),","path":"scripts/vlm/issue_1670_reconcile.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"chatgpt-codex-connector[bot]","id":199175422,"node_id":"BOT_kgDOC98s_g","avatar_url":"https://avatars.githubusercontent.com/in/1144995?v=4","gravatar_id":"","url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D","html_url":"https://github.com/apps/chatgpt-codex-connector","followers_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/followers","following_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/following{/other_user}","gists_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/gists{/gist_id}","starred_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/subscriptions","organizations_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/orgs","repos_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/repos","events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/events{/privacy}","received_events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/received_events","type":"Bot","user_view_type":"public","site_admin":false},"body":"**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Require known final states before certifying topology routing**\n\nWhen checker import runs before the pre-resolution ledger exists, every topology row has `final_state=None`, but this expression still declares all findings safely routed. The committed receipt demonstrates that path: all 152 rows retain null final states while `topology_findings_routed_to_disagreement_or_safe_rejection` is `true`; later replays compare only lane/question/error fields and do not refresh that evidence. Consequently a later `verified_agreement` assignment for a topology finding would not invalidate the frozen pass receipt, so routing should remain unverified until every row has a concrete permitted state.\n\nUseful? React with 👍 / 👎.","created_at":"2026-07-22T18:23:46Z","updated_at":"2026-07-22T18:23:46Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753258","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753258"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753258"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753258/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":377,"original_start_line":349,"start_side":"RIGHT","line":378,"original_line":350,"side":"RIGHT","author_association":"NONE","original_position":350,"position":378,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753263","pull_request_review_id":4757470513,"id":3632753263,"node_id":"PRRC_kwDOO5D6p87Yh2pv","diff_hunk":"@@ -1717,12 +1718,22 @@ def finalize_maker(_args: argparse.Namespace) -> dict[str, Any]:\n         if row[\"state\"] == \"provider_or_transport_failure\":\n             if row[\"full_pass\"][\"classification\"] == row[\"mark_pass\"][\"classification\"] == \"complete\" and row[\"normalized_candidate\"] is None:\n                 corrected[\"state\"] = \"safe_rejection\"\n+                corrected[\"safe_rejection_reason\"] = \"maker_normalization_rejected\"\n                 corrections.append({\n                     \"question_id\": row[\"question_id\"], \"original_state\": row[\"state\"],\n                     \"corrected_state\": \"safe_rejection\",\n                     \"reason\": \"both_provider_passes_complete_but_normalization_rejected_inconsistent_structure\",\n                     \"original_record_sha256\": row[\"record_sha256\"],\n                 })\n+            elif FAIL_CLOSED_TERMINAL_PROVIDER_FAILURES:\n+                corrected[\"state\"] = \"safe_rejection\"","path":"scripts/vlm/issue_1653_structural_batch_001.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"chatgpt-codex-connector[bot]","id":199175422,"node_id":"BOT_kgDOC98s_g","avatar_url":"https://avatars.githubusercontent.com/in/1144995?v=4","gravatar_id":"","url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D","html_url":"https://github.com/apps/chatgpt-codex-connector","followers_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/followers","following_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/following{/other_user}","gists_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/gists{/gist_id}","starred_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/subscriptions","organizations_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/orgs","repos_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/repos","events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/events{/privacy}","received_events_url":"https://api.github.com/users/chatgpt-codex-connector%5Bbot%5D/received_events","type":"Bot","user_view_type":"public","site_admin":false},"body":"**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Refresh the active freeze after changing maker code**\n\nThe committed active `pre-provider-freeze.v2.json` still binds `scripts/vlm/issue_1653_structural_batch_001.py` to SHA-256 `bd9ac0…`, but this commit changes that file to `7f4a26…`; it likewise binds the Batch005 adapter to a hash that no longer matches. As a result, loading the committed active freeze and calling `verify_frozen_dependencies` now always raises `pre_provider_dependency_hash_drift_global_stop`, so the advertised maker run cannot be resumed or reproduced under its own frozen authority. Regenerate and reactivate the freeze after these behavior changes, before relying on the resulting provider artifacts.\n\nUseful? React with 👍 / 👎.","created_at":"2026-07-22T18:23:46Z","updated_at":"2026-07-22T18:23:46Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753263","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753263"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632753263"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632753263/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":1728,"original_start_line":1728,"start_side":"RIGHT","line":1729,"original_line":1729,"side":"RIGHT","author_association":"NONE","original_position":20,"position":20,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632806573","pull_request_review_id":4757538802,"id":3632806573,"node_id":"PRRC_kwDOO5D6p87YiDqt","diff_hunk":"@@ -0,0 +1,695 @@\n+#!/usr/bin/env python3\n+\"\"\"Import, validate, compare, and prepare resolution for Batch005 checkers.\"\"\"\n+from __future__ import annotations\n+\n+from collections import Counter\n+import argparse\n+import hashlib\n+import json\n+from pathlib import Path\n+from typing import Any\n+\n+from jsonschema import Draft202012Validator\n+\n+\n+REPO = Path(__file__).resolve().parents[2]\n+ISSUE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-structural-batch-005-1670\"\n+FREEZE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-production-freeze-1650\"\n+EXTERNAL_ROOT = Path.home() / \".local/share/cie9709/full-corpus/9709-structural-batch-005\"\n+BATCH_ID = \"9709-structural-batch-005\"\n+PROTOCOL_SHA256 = \"fba7d4e4d5a12574cdb78fe1a9c3fe30c0c3fd03ae717dde4c5caa81b54fd2ab\"\n+PRIMARY = {\n+    \"checker-shard-01\": (\"ao-checker-01\", 77),\n+    \"checker-shard-02\": (\"ao-checker-02\", 74),\n+    \"checker-shard-03\": (\"ao-checker-03\", 76),\n+}\n+SECOND = {\n+    \"checker-shard-01\": \"ao-checker-01-second-source-read\",\n+    \"checker-shard-02\": \"ao-checker-02b\",\n+    \"checker-shard-03\": \"ao-checker-03b\",\n+}\n+FORBIDDEN_KEYS = {\n+    \"maker_candidate\", \"maker_output\", \"maker_warnings\", \"expected_structure\",\n+    \"historical_gold\", \"gold\", \"truth\", \"other_checker_outputs\", \"resolver_output\",\n+}\n+\n+\n+def canonical_bytes(value: Any) -> bytes:\n+    return json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\")).encode()\n+\n+\n+def frozen_serialize_bytes(value: Any) -> bytes:\n+    \"\"\"Match the frozen JS `serialize`: two-space JSON plus trailing newline.\"\"\"\n+    return (json.dumps(value, ensure_ascii=False, indent=2, separators=(\",\", \": \")) + \"\\n\").encode()\n+\n+\n+def sha256_bytes(value: bytes) -> str:\n+    return hashlib.sha256(value).hexdigest()\n+\n+\n+def sha256_file(path: Path) -> str:\n+    return sha256_bytes(path.read_bytes())\n+\n+\n+def read_json(path: Path) -> Any:\n+    return json.loads(path.read_text(encoding=\"utf-8\"))\n+\n+\n+def write_once(path: Path, value: Any) -> None:\n+    payload = canonical_bytes(value) + b\"\\n\"\n+    if path.exists():\n+        if path.read_bytes() != payload:\n+            raise RuntimeError(f\"write_once_mismatch:{path}\")\n+        return\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    with path.open(\"xb\") as handle:\n+        handle.write(payload)\n+\n+\n+def record_sha256(value: dict[str, Any]) -> str:\n+    payload = json.loads(json.dumps(value))\n+    del payload[\"provenance\"][\"record_sha256\"]\n+    return sha256_bytes(canonical_bytes(payload))\n+\n+\n+def recursive_keys(value: Any) -> set[str]:\n+    if isinstance(value, dict):\n+        return set(value) | set().union(*(recursive_keys(item) for item in value.values()))\n+    if isinstance(value, list):\n+        return set().union(*(recursive_keys(item) for item in value)) if value else set()\n+    return set()\n+\n+\n+def extraction_validator() -> Draft202012Validator:\n+    schema = read_json(FREEZE_ROOT / \"production-objects.schema.v1.json\")\n+    return Draft202012Validator({\n+        \"$schema\": schema[\"$schema\"], \"$defs\": schema[\"$defs\"],\n+        \"$ref\": \"#/$defs/extraction_output\",\n+    })\n+\n+\n+def topology_errors(value: dict[str, Any]) -> list[str]:\n+    nodes = value[\"structural_nodes\"]\n+    paths = [node[\"owner_path\"] for node in nodes]\n+    errors = []\n+    if len(paths) != len(set(paths)):\n+        errors.append(\"duplicate_owner_path\")\n+    owner_set = set(paths)\n+    root = value[\"question_id\"].rsplit(\"_q\", 1)[1]\n+    for node in nodes:\n+        segments = node[\"owner_path\"].split(\"/\")\n+        expected_parent = \"/\".join(segments[:-1]) if len(segments) > 1 else None\n+        expected_label = str(int(root)) if len(segments) == 1 else segments[-1]\n+        observed_label = str(node[\"printed_label\"]).strip().removeprefix(\"(\").removesuffix(\")\").lower()\n+        if segments[0] != f\"q{root}\": errors.append(f\"{node['owner_path']}:question_root\")\n+        if node[\"parent_owner_path\"] != expected_parent: errors.append(f\"{node['owner_path']}:parent\")\n+        if expected_parent is not None and expected_parent not in owner_set: errors.append(f\"{node['owner_path']}:orphan\")\n+        if node[\"whole_question_alias\"] != node[\"owner_path\"].endswith(\"/$whole\"): errors.append(f\"{node['owner_path']}:whole_alias\")\n+        if \"$whole\" in segments[:-1]: errors.append(f\"{node['owner_path']}:whole_descendant\")\n+        if observed_label != expected_label.lower(): errors.append(f\"{node['owner_path']}:printed_label\")\n+    return errors\n+\n+\n+def evidence_row_key(row: dict[str, Any]) -> tuple:\n+    return (\n+        row.get(\"source_object_id\"), row.get(\"source_sha256\"), row.get(\"page_number\"),\n+        row.get(\"page_image_sha256\"), row.get(\"crop_sha256\"),\n+    )\n+\n+\n+def terminal_summary(root: Path) -> Path:\n+    rows = sorted(root.glob(\"terminal*.json\"))\n+    if len(rows) > 1 and all(path.name.startswith(\"terminal-receipt.v\") for path in rows):\n+        return rows[-1]\n+    if len(rows) != 1:\n+        raise RuntimeError(f\"terminal_summary_membership:{root}:{len(rows)}\")\n+    return rows[0]\n+\n+\n+def record_files(root: Path) -> list[Path]:\n+    return sorted(path for path in (root / \"records\").rglob(\"*.json\") if path.is_file())\n+\n+\n+def validate_lane(\n+    *, shard_id: str, worker_id: str, expected_ids: set[str], root: Path,\n+    packet_by_id: dict[str, dict], bundle_by_id: dict[str, dict], validator: Draft202012Validator,\n+    official_inventory_sha256: str, source_inventory_sha256: str,\n+    allow_expected_superset: bool = False,\n+) -> dict[str, Any]:\n+    summary_path = terminal_summary(root)\n+    summary = read_json(summary_path)\n+    if (\n+        summary.get(\"candidate_blind\") is not True\n+        or summary.get(\"candidate_responses_used\") is True\n+        or summary.get(\"shard_id\") != shard_id\n+        or summary.get(\"worker_id\") != worker_id\n+    ):\n+        raise RuntimeError(f\"terminal_summary_invalid:{shard_id}:{worker_id}\")\n+    files = record_files(root)\n+    values = [read_json(path) for path in files]\n+    ids = [row.get(\"question_id\") for row in values]\n+    membership_valid = set(ids) >= expected_ids if allow_expected_superset else set(ids) == expected_ids\n+    if len(ids) != len(set(ids)) or not membership_valid:\n+        raise RuntimeError(f\"lane_membership:{shard_id}:{len(ids)}:{len(expected_ids)}\")","path":"scripts/vlm/issue_1670_reconcile.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"Samsen879","id":172257175,"node_id":"U_kgDOCkRvlw","avatar_url":"https://avatars.githubusercontent.com/u/172257175?v=4","gravatar_id":"","url":"https://api.github.com/users/Samsen879","html_url":"https://github.com/Samsen879","followers_url":"https://api.github.com/users/Samsen879/followers","following_url":"https://api.github.com/users/Samsen879/following{/other_user}","gists_url":"https://api.github.com/users/Samsen879/gists{/gist_id}","starred_url":"https://api.github.com/users/Samsen879/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/Samsen879/subscriptions","organizations_url":"https://api.github.com/users/Samsen879/orgs","repos_url":"https://api.github.com/users/Samsen879/repos","events_url":"https://api.github.com/users/Samsen879/events{/privacy}","received_events_url":"https://api.github.com/users/Samsen879/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Fixed at a4ab68010. Import now requires the aggregate second-read IDs to have observed count 227, unique count 227, and exact equality with the 227 packet IDs; any duplicate, missing, or foreign ID fails closed. checker-import-and-validation-receipt.v2.json records the exact/disjoint proof, and the focused regression verifies all zero discrepancy counts.","created_at":"2026-07-22T18:31:58Z","updated_at":"2026-07-22T18:31:58Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632806573","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632806573"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632806573"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632806573/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":151,"original_start_line":151,"start_side":"RIGHT","line":153,"original_line":153,"side":"RIGHT","in_reply_to_id":3632753252,"author_association":"OWNER","original_position":153,"position":153,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632806995","pull_request_review_id":4757539469,"id":3632806995,"node_id":"PRRC_kwDOO5D6p87YiDxT","diff_hunk":"@@ -0,0 +1,695 @@\n+#!/usr/bin/env python3\n+\"\"\"Import, validate, compare, and prepare resolution for Batch005 checkers.\"\"\"\n+from __future__ import annotations\n+\n+from collections import Counter\n+import argparse\n+import hashlib\n+import json\n+from pathlib import Path\n+from typing import Any\n+\n+from jsonschema import Draft202012Validator\n+\n+\n+REPO = Path(__file__).resolve().parents[2]\n+ISSUE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-structural-batch-005-1670\"\n+FREEZE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-production-freeze-1650\"\n+EXTERNAL_ROOT = Path.home() / \".local/share/cie9709/full-corpus/9709-structural-batch-005\"\n+BATCH_ID = \"9709-structural-batch-005\"\n+PROTOCOL_SHA256 = \"fba7d4e4d5a12574cdb78fe1a9c3fe30c0c3fd03ae717dde4c5caa81b54fd2ab\"\n+PRIMARY = {\n+    \"checker-shard-01\": (\"ao-checker-01\", 77),\n+    \"checker-shard-02\": (\"ao-checker-02\", 74),\n+    \"checker-shard-03\": (\"ao-checker-03\", 76),\n+}\n+SECOND = {\n+    \"checker-shard-01\": \"ao-checker-01-second-source-read\",\n+    \"checker-shard-02\": \"ao-checker-02b\",\n+    \"checker-shard-03\": \"ao-checker-03b\",\n+}\n+FORBIDDEN_KEYS = {\n+    \"maker_candidate\", \"maker_output\", \"maker_warnings\", \"expected_structure\",\n+    \"historical_gold\", \"gold\", \"truth\", \"other_checker_outputs\", \"resolver_output\",\n+}\n+\n+\n+def canonical_bytes(value: Any) -> bytes:\n+    return json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\")).encode()\n+\n+\n+def frozen_serialize_bytes(value: Any) -> bytes:\n+    \"\"\"Match the frozen JS `serialize`: two-space JSON plus trailing newline.\"\"\"\n+    return (json.dumps(value, ensure_ascii=False, indent=2, separators=(\",\", \": \")) + \"\\n\").encode()\n+\n+\n+def sha256_bytes(value: bytes) -> str:\n+    return hashlib.sha256(value).hexdigest()\n+\n+\n+def sha256_file(path: Path) -> str:\n+    return sha256_bytes(path.read_bytes())\n+\n+\n+def read_json(path: Path) -> Any:\n+    return json.loads(path.read_text(encoding=\"utf-8\"))\n+\n+\n+def write_once(path: Path, value: Any) -> None:\n+    payload = canonical_bytes(value) + b\"\\n\"\n+    if path.exists():\n+        if path.read_bytes() != payload:\n+            raise RuntimeError(f\"write_once_mismatch:{path}\")\n+        return\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    with path.open(\"xb\") as handle:\n+        handle.write(payload)\n+\n+\n+def record_sha256(value: dict[str, Any]) -> str:\n+    payload = json.loads(json.dumps(value))\n+    del payload[\"provenance\"][\"record_sha256\"]\n+    return sha256_bytes(canonical_bytes(payload))\n+\n+\n+def recursive_keys(value: Any) -> set[str]:\n+    if isinstance(value, dict):\n+        return set(value) | set().union(*(recursive_keys(item) for item in value.values()))\n+    if isinstance(value, list):\n+        return set().union(*(recursive_keys(item) for item in value)) if value else set()\n+    return set()\n+\n+\n+def extraction_validator() -> Draft202012Validator:\n+    schema = read_json(FREEZE_ROOT / \"production-objects.schema.v1.json\")\n+    return Draft202012Validator({\n+        \"$schema\": schema[\"$schema\"], \"$defs\": schema[\"$defs\"],\n+        \"$ref\": \"#/$defs/extraction_output\",\n+    })\n+\n+\n+def topology_errors(value: dict[str, Any]) -> list[str]:\n+    nodes = value[\"structural_nodes\"]\n+    paths = [node[\"owner_path\"] for node in nodes]\n+    errors = []\n+    if len(paths) != len(set(paths)):\n+        errors.append(\"duplicate_owner_path\")\n+    owner_set = set(paths)\n+    root = value[\"question_id\"].rsplit(\"_q\", 1)[1]\n+    for node in nodes:\n+        segments = node[\"owner_path\"].split(\"/\")\n+        expected_parent = \"/\".join(segments[:-1]) if len(segments) > 1 else None\n+        expected_label = str(int(root)) if len(segments) == 1 else segments[-1]\n+        observed_label = str(node[\"printed_label\"]).strip().removeprefix(\"(\").removesuffix(\")\").lower()\n+        if segments[0] != f\"q{root}\": errors.append(f\"{node['owner_path']}:question_root\")\n+        if node[\"parent_owner_path\"] != expected_parent: errors.append(f\"{node['owner_path']}:parent\")\n+        if expected_parent is not None and expected_parent not in owner_set: errors.append(f\"{node['owner_path']}:orphan\")\n+        if node[\"whole_question_alias\"] != node[\"owner_path\"].endswith(\"/$whole\"): errors.append(f\"{node['owner_path']}:whole_alias\")\n+        if \"$whole\" in segments[:-1]: errors.append(f\"{node['owner_path']}:whole_descendant\")\n+        if observed_label != expected_label.lower(): errors.append(f\"{node['owner_path']}:printed_label\")\n+    return errors\n+\n+\n+def evidence_row_key(row: dict[str, Any]) -> tuple:\n+    return (\n+        row.get(\"source_object_id\"), row.get(\"source_sha256\"), row.get(\"page_number\"),\n+        row.get(\"page_image_sha256\"), row.get(\"crop_sha256\"),\n+    )\n+\n+\n+def terminal_summary(root: Path) -> Path:\n+    rows = sorted(root.glob(\"terminal*.json\"))\n+    if len(rows) > 1 and all(path.name.startswith(\"terminal-receipt.v\") for path in rows):\n+        return rows[-1]\n+    if len(rows) != 1:\n+        raise RuntimeError(f\"terminal_summary_membership:{root}:{len(rows)}\")\n+    return rows[0]\n+\n+\n+def record_files(root: Path) -> list[Path]:\n+    return sorted(path for path in (root / \"records\").rglob(\"*.json\") if path.is_file())\n+\n+\n+def validate_lane(\n+    *, shard_id: str, worker_id: str, expected_ids: set[str], root: Path,\n+    packet_by_id: dict[str, dict], bundle_by_id: dict[str, dict], validator: Draft202012Validator,\n+    official_inventory_sha256: str, source_inventory_sha256: str,\n+    allow_expected_superset: bool = False,\n+) -> dict[str, Any]:\n+    summary_path = terminal_summary(root)\n+    summary = read_json(summary_path)\n+    if (\n+        summary.get(\"candidate_blind\") is not True\n+        or summary.get(\"candidate_responses_used\") is True\n+        or summary.get(\"shard_id\") != shard_id\n+        or summary.get(\"worker_id\") != worker_id\n+    ):\n+        raise RuntimeError(f\"terminal_summary_invalid:{shard_id}:{worker_id}\")\n+    files = record_files(root)\n+    values = [read_json(path) for path in files]\n+    ids = [row.get(\"question_id\") for row in values]\n+    membership_valid = set(ids) >= expected_ids if allow_expected_superset else set(ids) == expected_ids\n+    if len(ids) != len(set(ids)) or not membership_valid:\n+        raise RuntimeError(f\"lane_membership:{shard_id}:{len(ids)}:{len(expected_ids)}\")\n+    rows = []\n+    dialects = Counter()\n+    for path, value in zip(files, values, strict=True):\n+        question_id = value[\"question_id\"]\n+        packet_row = packet_by_id[question_id]\n+        bundle_row = bundle_by_id[question_id]\n+        packet = read_json(Path(packet_row[\"path\"]))\n+        bundle = read_json(Path(bundle_row[\"path\"]))\n+        evidence_path = Path(bundle[\"evidence_manifest_path\"])\n+        evidence = read_json(evidence_path)\n+        evidence_hash = sha256_bytes(frozen_serialize_bytes({\"schema_version\": evidence[\"schema_version\"], \"rows\": evidence[\"rows\"]}))\n+        errors = sorted(validator.iter_errors(value), key=lambda error: list(error.path))\n+        if errors:\n+            raise RuntimeError(f\"schema_invalid:{question_id}:{errors[0].json_path}:{errors[0].message}\")\n+        relational_errors = topology_errors(value)\n+        if recursive_keys(value) & FORBIDDEN_KEYS:\n+            raise RuntimeError(f\"candidate_blindness_invalid:{question_id}\")\n+        producer_record_hash = value[\"provenance\"][\"record_sha256\"]\n+        if producer_record_hash == record_sha256(value):\n+            record_hash_dialect = \"canonical_record_without_record_sha256\"\n+        elif isinstance(producer_record_hash, str) and len(producer_record_hash) == 64 and all(\n+            character in \"0123456789abcdef\" for character in producer_record_hash\n+        ):\n+            record_hash_dialect = \"opaque_producer_hash_with_integration_file_sha256\"\n+        else:\n+            raise RuntimeError(f\"record_hash_shape_invalid:{question_id}\")\n+        dialects[record_hash_dialect] += 1\n+        accepted_shard_ids = {shard_id}\n+        if allow_expected_superset:\n+            accepted_shard_ids.add(f\"{shard_id}-second-read\")\n+        provenance_shard_id = value[\"provenance\"][\"shard_id\"]\n+        dialects[\n+            \"second_read_shard_suffix\" if provenance_shard_id != shard_id\n+            else \"canonical_shard_id\"\n+        ] += 1\n+        if (\n+            sha256_file(Path(packet_row[\"path\"])) != packet_row[\"sha256\"]\n+            or sha256_file(Path(bundle_row[\"path\"])) != bundle_row[\"sha256\"]\n+            or evidence_hash != evidence[\"manifest_sha256\"] != \"\"\n+            or evidence_hash != bundle[\"evidence_manifest_sha256\"]\n+            or value[\"question\"][\"question_id\"] != question_id\n+            or value[\"question\"][\"paper_id\"] != packet[\"paper_id\"]\n+            or value[\"question\"][\"verification_state\"] != \"checker_complete\"\n+            or value[\"question\"][\"complete_page_chain\"] != [row[\"page_number\"] for row in bundle[\"qp_complete_page_chain\"]]\n+            or value[\"question\"][\"source_membership\"] != [packet[\"source_object_ids\"][\"qp\"], packet[\"source_object_ids\"][\"ms\"]]\n+            or value[\"provenance\"][\"worker_id\"] != worker_id\n+            or provenance_shard_id not in accepted_shard_ids\n+            or value[\"provenance\"][\"protocol_sha256\"] != PROTOCOL_SHA256\n+        ):\n+            raise RuntimeError(f\"identity_or_authority_invalid:{question_id}\")\n+        inventory_value = value[\"question\"][\"provenance\"][\"inventory_sha256\"]\n+        if inventory_value == official_inventory_sha256:\n+            dialects[\"official_inventory_file_sha256\"] += 1\n+        elif inventory_value == summary.get(\"inventory_sha256\"):\n+            dialects[\"producer_terminal_inventory_sha256\"] += 1\n+        else:\n+            raise RuntimeError(f\"inventory_authority_unbound:{question_id}\")\n+        input_value = value[\"provenance\"][\"input_manifest_sha256\"]\n+        input_dialects = {\n+            evidence_hash: \"evidence_manifest_sha256\",\n+            packet_row[\"sha256\"]: \"canonical_packet_file_sha256\",\n+            bundle_row[\"sha256\"]: \"visual_bundle_file_sha256\",\n+            bundle.get(\"canonical_packet_sha256\"): \"canonical_packet_file_sha256\",\n+            source_inventory_sha256: \"source_inventory_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-packet-manifest.v5.json\"):\n+                \"checker_packet_manifest_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\"):\n+                \"checker_visual_bundle_manifest_file_sha256\",\n+        }\n+        if input_value not in input_dialects:\n+            raise RuntimeError(f\"input_authority_unbound:{question_id}:{input_value}\")\n+        dialects[input_dialects[input_value]] += 1\n+        official_rows = {evidence_row_key(row) for row in evidence[\"rows\"]}\n+        claimed_rows = [*value[\"question\"][\"authoritative_regions\"]]\n+        for node in value[\"structural_nodes\"]:\n+            claimed_rows.extend(node[\"page_region_evidence\"])\n+            claimed_rows.extend(node[\"ms_evidence\"])\n+        if any(evidence_row_key(row) not in official_rows for row in claimed_rows):\n+            raise RuntimeError(f\"source_evidence_unbound:{question_id}\")\n+        findings = sorted({\n+            warning.split(\":\", 1)[1] for warning in value.get(\"warnings\", [])\n+            if warning.startswith(\"structural_relational_finding:\")\n+        })\n+        rows.append({\n+            \"question_id\": question_id, \"paper_id\": value[\"question\"][\"paper_id\"],\n+            \"path\": str(path), \"file_sha256\": sha256_file(path),\n+            \"record_sha256\": producer_record_hash, \"record_hash_dialect\": record_hash_dialect,\n+            \"input_authority_sha256\": input_value,\n+            \"input_authority_dialect\": input_dialects[input_value],\n+            \"inventory_authority_sha256\": inventory_value,\n+            \"resolver_required_relational_findings\": findings,\n+        })\n+    rows.sort(key=lambda row: row[\"question_id\"])\n+    return {\n+        \"shard_id\": shard_id, \"worker_id\": worker_id, \"question_count\": len(rows),\n+        \"root\": str(root), \"terminal_summary\": {\"path\": str(summary_path), \"sha256\": sha256_file(summary_path)},\n+        \"record_set_sha256\": sha256_bytes(canonical_bytes([[row[\"question_id\"], row[\"file_sha256\"]] for row in rows])),\n+        \"authority_dialect_counts\": dict(sorted(dialects.items())), \"records\": rows,\n+    }\n+\n+\n+def import_checkers(_args: argparse.Namespace) -> dict[str, Any]:\n+    packet_manifest = read_json(ISSUE_ROOT / \"checker-packet-manifest.v5.json\")\n+    bundle_manifest = read_json(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\")\n+    packet_by_id = {row[\"question_id\"]: row for row in packet_manifest[\"packets\"]}\n+    bundle_by_id = {row[\"question_id\"]: row for row in bundle_manifest[\"bundles\"]}\n+    if len(packet_by_id) != len(bundle_by_id) != 0 or set(packet_by_id) != set(bundle_by_id):\n+        raise RuntimeError(\"packet_bundle_membership_invalid\")\n+    official_inventory_sha256 = sha256_file(FREEZE_ROOT / \"inventory.v1.json\")\n+    source_inventory_sha256 = sha256_file(ISSUE_ROOT / \"source-evidence-inventory.v2.json\")\n+    validator = extraction_validator()\n+    expected_by_shard = {\n+        shard_id: {row[\"question_id\"] for row in packet_manifest[\"packets\"] if row[\"shard_id\"] == shard_id}\n+        for shard_id in PRIMARY\n+    }\n+    high_risk_by_shard = {shard_id: set() for shard_id in PRIMARY}\n+    for row in bundle_manifest[\"bundles\"]:\n+        bundle = read_json(Path(row[\"path\"]))\n+        if bundle[\"layout_risk_triggers\"] != [\"ordinary_single_page\"]:\n+            high_risk_by_shard[row[\"shard_id\"]].add(row[\"question_id\"])\n+    primary = []\n+    second = []\n+    for shard_id, (worker_id, expected) in PRIMARY.items():\n+        if len(expected_by_shard[shard_id]) != expected:\n+            raise RuntimeError(f\"primary_expected_count:{shard_id}\")\n+        primary.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=expected_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-outputs-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+        ))\n+    for shard_id, worker_id in SECOND.items():\n+        second.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=high_risk_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-second-reads-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+            allow_expected_superset=True,\n+        ))\n+    primary_count = sum(row[\"question_count\"] for row in primary)\n+    second_count = sum(row[\"question_count\"] for row in second)\n+    high_risk_counts = {shard_id: len(rows) for shard_id, rows in high_risk_by_shard.items()}\n+    if primary_count != 227 or second_count != 227 or sum(high_risk_counts.values()) != 179:\n+        raise RuntimeError(f\"checker_conservation:{primary_count}:{second_count}:{high_risk_counts}\")\n+    all_records = [record for lane in [*primary, *second] for record in lane[\"records\"]]\n+    finding_records = [row for row in all_records if row[\"resolver_required_relational_findings\"]]\n+    primary_receipt = {\n+        \"schema_version\": \"9709_batch_005_primary_checker_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"question_count\": primary_count, \"paper_count\": 27, \"shards\": primary,\n+    }\n+    second_receipt = {\n+        \"schema_version\": \"9709_batch_005_independent_second_read_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"primary_outputs_read_by_second_readers\": False,\n+        \"source_authoritative_high_risk_count\": 179, \"second_read_count\": second_count,\n+        \"additional_risk_stratified_sample_read_count\": second_count - 179,\n+        \"shard_counts\": high_risk_counts, \"shards\": second,\n+    }\n+    existing_import_path = ISSUE_ROOT / \"checker-import-and-validation-receipt.v1.json\"\n+    frozen_generator = read_json(existing_import_path)[\"generator\"] if existing_import_path.exists() else {\"path\": str(Path(__file__).relative_to(REPO)), \"sha256\": sha256_file(Path(__file__))}","path":"scripts/vlm/issue_1670_reconcile.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"Samsen879","id":172257175,"node_id":"U_kgDOCkRvlw","avatar_url":"https://avatars.githubusercontent.com/u/172257175?v=4","gravatar_id":"","url":"https://api.github.com/users/Samsen879","html_url":"https://github.com/Samsen879","followers_url":"https://api.github.com/users/Samsen879/followers","following_url":"https://api.github.com/users/Samsen879/following{/other_user}","gists_url":"https://api.github.com/users/Samsen879/gists{/gist_id}","starred_url":"https://api.github.com/users/Samsen879/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/Samsen879/subscriptions","organizations_url":"https://api.github.com/users/Samsen879/orgs","repos_url":"https://api.github.com/users/Samsen879/repos","events_url":"https://api.github.com/users/Samsen879/events{/privacy}","received_events_url":"https://api.github.com/users/Samsen879/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Fixed additively at a4ab68010 without rewriting v1. checker-import-and-validation-receipt.v2.json supersedes the frozen v1 receipt and binds the current validator SHA-256 9ca782177de458fd656ed5cdfd8aaef8138efe94cbb43f9d44d2c8cf4a54619c. deterministic-reconstruction.v2.json and artifact-manifest.v2.json bind the corrected receipt; the regression compares recorded provenance to the current file hash.","created_at":"2026-07-22T18:32:02Z","updated_at":"2026-07-22T18:32:02Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632806995","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632806995"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632806995"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632806995/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":323,"original_start_line":313,"start_side":"RIGHT","line":324,"original_line":314,"side":"RIGHT","in_reply_to_id":3632753255,"author_association":"OWNER","original_position":314,"position":324,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632807363","pull_request_review_id":4757539987,"id":3632807363,"node_id":"PRRC_kwDOO5D6p87YiD3D","diff_hunk":"@@ -0,0 +1,695 @@\n+#!/usr/bin/env python3\n+\"\"\"Import, validate, compare, and prepare resolution for Batch005 checkers.\"\"\"\n+from __future__ import annotations\n+\n+from collections import Counter\n+import argparse\n+import hashlib\n+import json\n+from pathlib import Path\n+from typing import Any\n+\n+from jsonschema import Draft202012Validator\n+\n+\n+REPO = Path(__file__).resolve().parents[2]\n+ISSUE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-structural-batch-005-1670\"\n+FREEZE_ROOT = REPO / \"artifacts/method-map/v1-9709/full-corpus-production-freeze-1650\"\n+EXTERNAL_ROOT = Path.home() / \".local/share/cie9709/full-corpus/9709-structural-batch-005\"\n+BATCH_ID = \"9709-structural-batch-005\"\n+PROTOCOL_SHA256 = \"fba7d4e4d5a12574cdb78fe1a9c3fe30c0c3fd03ae717dde4c5caa81b54fd2ab\"\n+PRIMARY = {\n+    \"checker-shard-01\": (\"ao-checker-01\", 77),\n+    \"checker-shard-02\": (\"ao-checker-02\", 74),\n+    \"checker-shard-03\": (\"ao-checker-03\", 76),\n+}\n+SECOND = {\n+    \"checker-shard-01\": \"ao-checker-01-second-source-read\",\n+    \"checker-shard-02\": \"ao-checker-02b\",\n+    \"checker-shard-03\": \"ao-checker-03b\",\n+}\n+FORBIDDEN_KEYS = {\n+    \"maker_candidate\", \"maker_output\", \"maker_warnings\", \"expected_structure\",\n+    \"historical_gold\", \"gold\", \"truth\", \"other_checker_outputs\", \"resolver_output\",\n+}\n+\n+\n+def canonical_bytes(value: Any) -> bytes:\n+    return json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(\",\", \":\")).encode()\n+\n+\n+def frozen_serialize_bytes(value: Any) -> bytes:\n+    \"\"\"Match the frozen JS `serialize`: two-space JSON plus trailing newline.\"\"\"\n+    return (json.dumps(value, ensure_ascii=False, indent=2, separators=(\",\", \": \")) + \"\\n\").encode()\n+\n+\n+def sha256_bytes(value: bytes) -> str:\n+    return hashlib.sha256(value).hexdigest()\n+\n+\n+def sha256_file(path: Path) -> str:\n+    return sha256_bytes(path.read_bytes())\n+\n+\n+def read_json(path: Path) -> Any:\n+    return json.loads(path.read_text(encoding=\"utf-8\"))\n+\n+\n+def write_once(path: Path, value: Any) -> None:\n+    payload = canonical_bytes(value) + b\"\\n\"\n+    if path.exists():\n+        if path.read_bytes() != payload:\n+            raise RuntimeError(f\"write_once_mismatch:{path}\")\n+        return\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    with path.open(\"xb\") as handle:\n+        handle.write(payload)\n+\n+\n+def record_sha256(value: dict[str, Any]) -> str:\n+    payload = json.loads(json.dumps(value))\n+    del payload[\"provenance\"][\"record_sha256\"]\n+    return sha256_bytes(canonical_bytes(payload))\n+\n+\n+def recursive_keys(value: Any) -> set[str]:\n+    if isinstance(value, dict):\n+        return set(value) | set().union(*(recursive_keys(item) for item in value.values()))\n+    if isinstance(value, list):\n+        return set().union(*(recursive_keys(item) for item in value)) if value else set()\n+    return set()\n+\n+\n+def extraction_validator() -> Draft202012Validator:\n+    schema = read_json(FREEZE_ROOT / \"production-objects.schema.v1.json\")\n+    return Draft202012Validator({\n+        \"$schema\": schema[\"$schema\"], \"$defs\": schema[\"$defs\"],\n+        \"$ref\": \"#/$defs/extraction_output\",\n+    })\n+\n+\n+def topology_errors(value: dict[str, Any]) -> list[str]:\n+    nodes = value[\"structural_nodes\"]\n+    paths = [node[\"owner_path\"] for node in nodes]\n+    errors = []\n+    if len(paths) != len(set(paths)):\n+        errors.append(\"duplicate_owner_path\")\n+    owner_set = set(paths)\n+    root = value[\"question_id\"].rsplit(\"_q\", 1)[1]\n+    for node in nodes:\n+        segments = node[\"owner_path\"].split(\"/\")\n+        expected_parent = \"/\".join(segments[:-1]) if len(segments) > 1 else None\n+        expected_label = str(int(root)) if len(segments) == 1 else segments[-1]\n+        observed_label = str(node[\"printed_label\"]).strip().removeprefix(\"(\").removesuffix(\")\").lower()\n+        if segments[0] != f\"q{root}\": errors.append(f\"{node['owner_path']}:question_root\")\n+        if node[\"parent_owner_path\"] != expected_parent: errors.append(f\"{node['owner_path']}:parent\")\n+        if expected_parent is not None and expected_parent not in owner_set: errors.append(f\"{node['owner_path']}:orphan\")\n+        if node[\"whole_question_alias\"] != node[\"owner_path\"].endswith(\"/$whole\"): errors.append(f\"{node['owner_path']}:whole_alias\")\n+        if \"$whole\" in segments[:-1]: errors.append(f\"{node['owner_path']}:whole_descendant\")\n+        if observed_label != expected_label.lower(): errors.append(f\"{node['owner_path']}:printed_label\")\n+    return errors\n+\n+\n+def evidence_row_key(row: dict[str, Any]) -> tuple:\n+    return (\n+        row.get(\"source_object_id\"), row.get(\"source_sha256\"), row.get(\"page_number\"),\n+        row.get(\"page_image_sha256\"), row.get(\"crop_sha256\"),\n+    )\n+\n+\n+def terminal_summary(root: Path) -> Path:\n+    rows = sorted(root.glob(\"terminal*.json\"))\n+    if len(rows) > 1 and all(path.name.startswith(\"terminal-receipt.v\") for path in rows):\n+        return rows[-1]\n+    if len(rows) != 1:\n+        raise RuntimeError(f\"terminal_summary_membership:{root}:{len(rows)}\")\n+    return rows[0]\n+\n+\n+def record_files(root: Path) -> list[Path]:\n+    return sorted(path for path in (root / \"records\").rglob(\"*.json\") if path.is_file())\n+\n+\n+def validate_lane(\n+    *, shard_id: str, worker_id: str, expected_ids: set[str], root: Path,\n+    packet_by_id: dict[str, dict], bundle_by_id: dict[str, dict], validator: Draft202012Validator,\n+    official_inventory_sha256: str, source_inventory_sha256: str,\n+    allow_expected_superset: bool = False,\n+) -> dict[str, Any]:\n+    summary_path = terminal_summary(root)\n+    summary = read_json(summary_path)\n+    if (\n+        summary.get(\"candidate_blind\") is not True\n+        or summary.get(\"candidate_responses_used\") is True\n+        or summary.get(\"shard_id\") != shard_id\n+        or summary.get(\"worker_id\") != worker_id\n+    ):\n+        raise RuntimeError(f\"terminal_summary_invalid:{shard_id}:{worker_id}\")\n+    files = record_files(root)\n+    values = [read_json(path) for path in files]\n+    ids = [row.get(\"question_id\") for row in values]\n+    membership_valid = set(ids) >= expected_ids if allow_expected_superset else set(ids) == expected_ids\n+    if len(ids) != len(set(ids)) or not membership_valid:\n+        raise RuntimeError(f\"lane_membership:{shard_id}:{len(ids)}:{len(expected_ids)}\")\n+    rows = []\n+    dialects = Counter()\n+    for path, value in zip(files, values, strict=True):\n+        question_id = value[\"question_id\"]\n+        packet_row = packet_by_id[question_id]\n+        bundle_row = bundle_by_id[question_id]\n+        packet = read_json(Path(packet_row[\"path\"]))\n+        bundle = read_json(Path(bundle_row[\"path\"]))\n+        evidence_path = Path(bundle[\"evidence_manifest_path\"])\n+        evidence = read_json(evidence_path)\n+        evidence_hash = sha256_bytes(frozen_serialize_bytes({\"schema_version\": evidence[\"schema_version\"], \"rows\": evidence[\"rows\"]}))\n+        errors = sorted(validator.iter_errors(value), key=lambda error: list(error.path))\n+        if errors:\n+            raise RuntimeError(f\"schema_invalid:{question_id}:{errors[0].json_path}:{errors[0].message}\")\n+        relational_errors = topology_errors(value)\n+        if recursive_keys(value) & FORBIDDEN_KEYS:\n+            raise RuntimeError(f\"candidate_blindness_invalid:{question_id}\")\n+        producer_record_hash = value[\"provenance\"][\"record_sha256\"]\n+        if producer_record_hash == record_sha256(value):\n+            record_hash_dialect = \"canonical_record_without_record_sha256\"\n+        elif isinstance(producer_record_hash, str) and len(producer_record_hash) == 64 and all(\n+            character in \"0123456789abcdef\" for character in producer_record_hash\n+        ):\n+            record_hash_dialect = \"opaque_producer_hash_with_integration_file_sha256\"\n+        else:\n+            raise RuntimeError(f\"record_hash_shape_invalid:{question_id}\")\n+        dialects[record_hash_dialect] += 1\n+        accepted_shard_ids = {shard_id}\n+        if allow_expected_superset:\n+            accepted_shard_ids.add(f\"{shard_id}-second-read\")\n+        provenance_shard_id = value[\"provenance\"][\"shard_id\"]\n+        dialects[\n+            \"second_read_shard_suffix\" if provenance_shard_id != shard_id\n+            else \"canonical_shard_id\"\n+        ] += 1\n+        if (\n+            sha256_file(Path(packet_row[\"path\"])) != packet_row[\"sha256\"]\n+            or sha256_file(Path(bundle_row[\"path\"])) != bundle_row[\"sha256\"]\n+            or evidence_hash != evidence[\"manifest_sha256\"] != \"\"\n+            or evidence_hash != bundle[\"evidence_manifest_sha256\"]\n+            or value[\"question\"][\"question_id\"] != question_id\n+            or value[\"question\"][\"paper_id\"] != packet[\"paper_id\"]\n+            or value[\"question\"][\"verification_state\"] != \"checker_complete\"\n+            or value[\"question\"][\"complete_page_chain\"] != [row[\"page_number\"] for row in bundle[\"qp_complete_page_chain\"]]\n+            or value[\"question\"][\"source_membership\"] != [packet[\"source_object_ids\"][\"qp\"], packet[\"source_object_ids\"][\"ms\"]]\n+            or value[\"provenance\"][\"worker_id\"] != worker_id\n+            or provenance_shard_id not in accepted_shard_ids\n+            or value[\"provenance\"][\"protocol_sha256\"] != PROTOCOL_SHA256\n+        ):\n+            raise RuntimeError(f\"identity_or_authority_invalid:{question_id}\")\n+        inventory_value = value[\"question\"][\"provenance\"][\"inventory_sha256\"]\n+        if inventory_value == official_inventory_sha256:\n+            dialects[\"official_inventory_file_sha256\"] += 1\n+        elif inventory_value == summary.get(\"inventory_sha256\"):\n+            dialects[\"producer_terminal_inventory_sha256\"] += 1\n+        else:\n+            raise RuntimeError(f\"inventory_authority_unbound:{question_id}\")\n+        input_value = value[\"provenance\"][\"input_manifest_sha256\"]\n+        input_dialects = {\n+            evidence_hash: \"evidence_manifest_sha256\",\n+            packet_row[\"sha256\"]: \"canonical_packet_file_sha256\",\n+            bundle_row[\"sha256\"]: \"visual_bundle_file_sha256\",\n+            bundle.get(\"canonical_packet_sha256\"): \"canonical_packet_file_sha256\",\n+            source_inventory_sha256: \"source_inventory_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-packet-manifest.v5.json\"):\n+                \"checker_packet_manifest_file_sha256\",\n+            sha256_file(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\"):\n+                \"checker_visual_bundle_manifest_file_sha256\",\n+        }\n+        if input_value not in input_dialects:\n+            raise RuntimeError(f\"input_authority_unbound:{question_id}:{input_value}\")\n+        dialects[input_dialects[input_value]] += 1\n+        official_rows = {evidence_row_key(row) for row in evidence[\"rows\"]}\n+        claimed_rows = [*value[\"question\"][\"authoritative_regions\"]]\n+        for node in value[\"structural_nodes\"]:\n+            claimed_rows.extend(node[\"page_region_evidence\"])\n+            claimed_rows.extend(node[\"ms_evidence\"])\n+        if any(evidence_row_key(row) not in official_rows for row in claimed_rows):\n+            raise RuntimeError(f\"source_evidence_unbound:{question_id}\")\n+        findings = sorted({\n+            warning.split(\":\", 1)[1] for warning in value.get(\"warnings\", [])\n+            if warning.startswith(\"structural_relational_finding:\")\n+        })\n+        rows.append({\n+            \"question_id\": question_id, \"paper_id\": value[\"question\"][\"paper_id\"],\n+            \"path\": str(path), \"file_sha256\": sha256_file(path),\n+            \"record_sha256\": producer_record_hash, \"record_hash_dialect\": record_hash_dialect,\n+            \"input_authority_sha256\": input_value,\n+            \"input_authority_dialect\": input_dialects[input_value],\n+            \"inventory_authority_sha256\": inventory_value,\n+            \"resolver_required_relational_findings\": findings,\n+        })\n+    rows.sort(key=lambda row: row[\"question_id\"])\n+    return {\n+        \"shard_id\": shard_id, \"worker_id\": worker_id, \"question_count\": len(rows),\n+        \"root\": str(root), \"terminal_summary\": {\"path\": str(summary_path), \"sha256\": sha256_file(summary_path)},\n+        \"record_set_sha256\": sha256_bytes(canonical_bytes([[row[\"question_id\"], row[\"file_sha256\"]] for row in rows])),\n+        \"authority_dialect_counts\": dict(sorted(dialects.items())), \"records\": rows,\n+    }\n+\n+\n+def import_checkers(_args: argparse.Namespace) -> dict[str, Any]:\n+    packet_manifest = read_json(ISSUE_ROOT / \"checker-packet-manifest.v5.json\")\n+    bundle_manifest = read_json(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\")\n+    packet_by_id = {row[\"question_id\"]: row for row in packet_manifest[\"packets\"]}\n+    bundle_by_id = {row[\"question_id\"]: row for row in bundle_manifest[\"bundles\"]}\n+    if len(packet_by_id) != len(bundle_by_id) != 0 or set(packet_by_id) != set(bundle_by_id):\n+        raise RuntimeError(\"packet_bundle_membership_invalid\")\n+    official_inventory_sha256 = sha256_file(FREEZE_ROOT / \"inventory.v1.json\")\n+    source_inventory_sha256 = sha256_file(ISSUE_ROOT / \"source-evidence-inventory.v2.json\")\n+    validator = extraction_validator()\n+    expected_by_shard = {\n+        shard_id: {row[\"question_id\"] for row in packet_manifest[\"packets\"] if row[\"shard_id\"] == shard_id}\n+        for shard_id in PRIMARY\n+    }\n+    high_risk_by_shard = {shard_id: set() for shard_id in PRIMARY}\n+    for row in bundle_manifest[\"bundles\"]:\n+        bundle = read_json(Path(row[\"path\"]))\n+        if bundle[\"layout_risk_triggers\"] != [\"ordinary_single_page\"]:\n+            high_risk_by_shard[row[\"shard_id\"]].add(row[\"question_id\"])\n+    primary = []\n+    second = []\n+    for shard_id, (worker_id, expected) in PRIMARY.items():\n+        if len(expected_by_shard[shard_id]) != expected:\n+            raise RuntimeError(f\"primary_expected_count:{shard_id}\")\n+        primary.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=expected_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-outputs-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+        ))\n+    for shard_id, worker_id in SECOND.items():\n+        second.append(validate_lane(\n+            shard_id=shard_id, worker_id=worker_id, expected_ids=high_risk_by_shard[shard_id],\n+            root=EXTERNAL_ROOT / \"checker-second-reads-v5\" / shard_id / f\"by-{worker_id}\",\n+            packet_by_id=packet_by_id, bundle_by_id=bundle_by_id, validator=validator,\n+            official_inventory_sha256=official_inventory_sha256, source_inventory_sha256=source_inventory_sha256,\n+            allow_expected_superset=True,\n+        ))\n+    primary_count = sum(row[\"question_count\"] for row in primary)\n+    second_count = sum(row[\"question_count\"] for row in second)\n+    high_risk_counts = {shard_id: len(rows) for shard_id, rows in high_risk_by_shard.items()}\n+    if primary_count != 227 or second_count != 227 or sum(high_risk_counts.values()) != 179:\n+        raise RuntimeError(f\"checker_conservation:{primary_count}:{second_count}:{high_risk_counts}\")\n+    all_records = [record for lane in [*primary, *second] for record in lane[\"records\"]]\n+    finding_records = [row for row in all_records if row[\"resolver_required_relational_findings\"]]\n+    primary_receipt = {\n+        \"schema_version\": \"9709_batch_005_primary_checker_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"question_count\": primary_count, \"paper_count\": 27, \"shards\": primary,\n+    }\n+    second_receipt = {\n+        \"schema_version\": \"9709_batch_005_independent_second_read_custody_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"candidate_blind\": True,\n+        \"primary_outputs_read_by_second_readers\": False,\n+        \"source_authoritative_high_risk_count\": 179, \"second_read_count\": second_count,\n+        \"additional_risk_stratified_sample_read_count\": second_count - 179,\n+        \"shard_counts\": high_risk_counts, \"shards\": second,\n+    }\n+    existing_import_path = ISSUE_ROOT / \"checker-import-and-validation-receipt.v1.json\"\n+    frozen_generator = read_json(existing_import_path)[\"generator\"] if existing_import_path.exists() else {\"path\": str(Path(__file__).relative_to(REPO)), \"sha256\": sha256_file(Path(__file__))}\n+    import_receipt = {\n+        \"schema_version\": \"9709_batch_005_checker_import_and_validation_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"generator\": frozen_generator,\n+        \"packet_manifest_sha256\": sha256_file(ISSUE_ROOT / \"checker-packet-manifest.v5.json\"),\n+        \"bundle_manifest_sha256\": sha256_file(ISSUE_ROOT / \"checker-visual-bundle-manifest.v5.json\"),\n+        \"primary_question_count\": primary_count, \"second_read_question_count\": second_count,\n+        \"schema_valid_count\": primary_count + second_count,\n+        \"source_evidence_identity_provenance_valid_count\": primary_count + second_count,\n+        \"resolver_required_relational_finding_record_count\": len(finding_records),\n+        \"resolver_required_relational_finding_question_count\": len({row[\"question_id\"] for row in finding_records}),\n+        \"candidate_blind_count\": primary_count + second_count, \"validation_error_count\": 0,\n+        \"checker_output_bytes_rewritten\": False, \"provider_calls_during_import\": 0,\n+        \"completed_checker_reads_repeated\": 0,\n+    }\n+    write_once(ISSUE_ROOT / \"checker-primary-custody.v1.json\", primary_receipt)\n+    write_once(ISSUE_ROOT / \"checker-second-read-custody.v1.json\", second_receipt)\n+    write_once(ISSUE_ROOT / \"checker-import-and-validation-receipt.v1.json\", import_receipt)\n+    topology_rows = []\n+    for lane_name, lanes in ((\"primary\", primary), (\"second_read\", second)):\n+        for lane in lanes:\n+            for record in lane[\"records\"]:\n+                errors = topology_errors(read_json(Path(record[\"path\"])))\n+                if errors:\n+                    topology_rows.append({\"lane\": lane_name, \"question_id\": record[\"question_id\"], \"errors\": errors,\n+                        \"final_state\": None})\n+    pre_path = ISSUE_ROOT / \"pre-resolution-verification-ledger.v1.json\"\n+    if pre_path.exists():\n+        states = {row[\"question_id\"]: row[\"state\"] for row in read_json(pre_path)[\"rows\"]}\n+        for row in topology_rows:\n+            row[\"final_state\"] = states[row[\"question_id\"]]\n+    topology_receipt = {\n+        \"schema_version\": \"9709_structural_batch_005_checker_topology_validation_v1\", \"issue\": 1670,\n+        \"batch_id\": BATCH_ID, \"status\": \"pass\", \"primary_schema_valid_count\": primary_count,\n+        \"second_read_schema_valid_count\": second_count, \"topology_finding_record_count\": len(topology_rows),\n+        \"verified_agreement_with_topology_finding_count\": sum(row[\"final_state\"] == \"verified_agreement\" for row in topology_rows),\n+        \"topology_findings_routed_to_disagreement_or_safe_rejection\": all(row[\"final_state\"] in {None, \"checker_disagreement\", \"safe_rejection\"} for row in topology_rows),","path":"scripts/vlm/issue_1670_reconcile.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"Samsen879","id":172257175,"node_id":"U_kgDOCkRvlw","avatar_url":"https://avatars.githubusercontent.com/u/172257175?v=4","gravatar_id":"","url":"https://api.github.com/users/Samsen879","html_url":"https://github.com/Samsen879","followers_url":"https://api.github.com/users/Samsen879/followers","following_url":"https://api.github.com/users/Samsen879/following{/other_user}","gists_url":"https://api.github.com/users/Samsen879/gists{/gist_id}","starred_url":"https://api.github.com/users/Samsen879/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/Samsen879/subscriptions","organizations_url":"https://api.github.com/users/Samsen879/orgs","repos_url":"https://api.github.com/users/Samsen879/repos","events_url":"https://api.github.com/users/Samsen879/events{/privacy}","received_events_url":"https://api.github.com/users/Samsen879/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Fixed additively at a4ab68010. checker-topology-validation.v2.json supersedes v1 and requires a concrete final state for every one of the 152 topology-finding records. Only resolver_verified or safe_rejection is permitted; verified_agreement count is zero, and any other or null state fails closed. The focused regression checks complete concrete coverage and permitted membership.","created_at":"2026-07-22T18:32:06Z","updated_at":"2026-07-22T18:32:06Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632807363","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632807363"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632807363"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632807363/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":377,"original_start_line":349,"start_side":"RIGHT","line":378,"original_line":350,"side":"RIGHT","in_reply_to_id":3632753258,"author_association":"OWNER","original_position":350,"position":378,"subject_type":"line"},{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632807830","pull_request_review_id":4757540643,"id":3632807830,"node_id":"PRRC_kwDOO5D6p87YiD-W","diff_hunk":"@@ -1717,12 +1718,22 @@ def finalize_maker(_args: argparse.Namespace) -> dict[str, Any]:\n         if row[\"state\"] == \"provider_or_transport_failure\":\n             if row[\"full_pass\"][\"classification\"] == row[\"mark_pass\"][\"classification\"] == \"complete\" and row[\"normalized_candidate\"] is None:\n                 corrected[\"state\"] = \"safe_rejection\"\n+                corrected[\"safe_rejection_reason\"] = \"maker_normalization_rejected\"\n                 corrections.append({\n                     \"question_id\": row[\"question_id\"], \"original_state\": row[\"state\"],\n                     \"corrected_state\": \"safe_rejection\",\n                     \"reason\": \"both_provider_passes_complete_but_normalization_rejected_inconsistent_structure\",\n                     \"original_record_sha256\": row[\"record_sha256\"],\n                 })\n+            elif FAIL_CLOSED_TERMINAL_PROVIDER_FAILURES:\n+                corrected[\"state\"] = \"safe_rejection\"","path":"scripts/vlm/issue_1653_structural_batch_001.py","commit_id":"a4ab68010004203cc687a7533273c7ef21ace8fe","original_commit_id":"5fdd72aeb19e69c9d7b01dcaa358a397b2a8b59d","user":{"login":"Samsen879","id":172257175,"node_id":"U_kgDOCkRvlw","avatar_url":"https://avatars.githubusercontent.com/u/172257175?v=4","gravatar_id":"","url":"https://api.github.com/users/Samsen879","html_url":"https://github.com/Samsen879","followers_url":"https://api.github.com/users/Samsen879/followers","following_url":"https://api.github.com/users/Samsen879/following{/other_user}","gists_url":"https://api.github.com/users/Samsen879/gists{/gist_id}","starred_url":"https://api.github.com/users/Samsen879/starred{/owner}{/repo}","subscriptions_url":"https://api.github.com/users/Samsen879/subscriptions","organizations_url":"https://api.github.com/users/Samsen879/orgs","repos_url":"https://api.github.com/users/Samsen879/repos","events_url":"https://api.github.com/users/Samsen879/events{/privacy}","received_events_url":"https://api.github.com/users/Samsen879/received_events","type":"User","user_view_type":"public","site_admin":false},"body":"Fixed additively at a4ab68010 without repeating production work. pre-provider-freeze.v3.json refreshes every dependency hash, preserves and binds immutable execution freeze v2 plus maker ledger and checkpoint custody, and active-pre-provider-freeze.v2.json activates v3. The adapter now selects that pointer; active freeze selection and verify_frozen_dependencies pass in the focused regression. review-corrections.v1.json records zero provider, maker, checker, or resolver calls and no rewritten production result.","created_at":"2026-07-22T18:32:10Z","updated_at":"2026-07-22T18:32:10Z","html_url":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632807830","pull_request_url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671","_links":{"self":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632807830"},"html":{"href":"https://github.com/Samsen879/ciecopilot-home/pull/1671#discussion_r3632807830"},"pull_request":{"href":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/1671"}},"reactions":{"url":"https://api.github.com/repos/Samsen879/ciecopilot-home/pulls/comments/3632807830/reactions","total_count":0,"+1":0,"-1":0,"laugh":0,"hooray":0,"confused":0,"heart":0,"rocket":0,"eyes":0},"start_line":1728,"original_start_line":1728,"start_side":"RIGHT","line":1729,"original_line":1729,"side":"RIGHT","in_reply_to_id":3632753263,"author_association":"OWNER","original_position":20,"position":20,"subject_type":"line"}]